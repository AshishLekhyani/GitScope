# Production requirements — lean build for free-tier hosting (Fly.io 256MB)
# torch/sentence-transformers are excluded: engine does full pattern analysis without them.
# Semantic RAG gracefully degrades to rule-based analysis when torch is absent.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
httpx>=0.27.0
aiofiles>=23.2.1
numpy>=1.26.0

# Vector memory (works without embeddings — stores text patterns, degrades gracefully)
chromadb>=0.5.0

# Static analysis
tree-sitter>=0.21.3
tree-sitter-languages>=1.10.2

# Utilities
semver>=3.0.0
packaging>=24.0
