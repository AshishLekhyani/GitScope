"""
Vulnerability Pattern Database
================================
A curated, constantly-updatable library of vulnerability signatures.
These are static patterns — the ML model learns NEW ones from the web crawler.

Pattern structure:
  id:          unique rule ID
  pattern:     regex (applied to added diff lines)
  severity:    critical | high | medium | low
  category:    secrets | injection | crypto | xss | auth | config | deps | ...
  description: message template ({file}, {match} interpolated)
  suggestion:  fix guidance
  cve_id:      linked CVE if applicable
  confidence:  0.0-1.0 (how sure we are — low for broad patterns)
  tags:        OWASP A-categories, CWE IDs

Covers OWASP Top 10 (2021):
  A01 Broken Access Control
  A02 Cryptographic Failures
  A03 Injection
  A04 Insecure Design
  A05 Security Misconfiguration
  A06 Vulnerable and Outdated Components  (handled by DependencyAgent)
  A07 Identification and Authentication Failures
  A08 Software and Data Integrity Failures
  A09 Security Logging and Monitoring Failures
  A10 Server-Side Request Forgery (SSRF)
"""

from __future__ import annotations

# ─────────────────────────────────────────────────────────────────────────────
# Main vulnerability pattern list
# ─────────────────────────────────────────────────────────────────────────────

VULN_PATTERNS: list[dict] = [

    # ── Secrets / Credentials ─────────────────────────────────────────────────
    {
        "id": "secret-aws-key",
        "pattern": r"AKIA[0-9A-Z]{16}",
        "severity": "critical", "category": "secrets",
        "description": "AWS Access Key ID in {file}. This credential exposed in VCS will be auto-harvested by bots within minutes.",
        "suggestion": "Rotate the key immediately in AWS IAM. Store in AWS Secrets Manager or environment variables. Add to .gitignore.",
        "cve_id": None, "confidence": 0.98,
        "tags": ["OWASP:A02", "CWE-312"],
    },
    {
        "id": "secret-aws-secret",
        "pattern": r"(?i)aws.{0,20}secret.{0,20}=.{0,5}['\"][A-Za-z0-9/+]{40}['\"]",
        "severity": "critical", "category": "secrets",
        "description": "AWS Secret Access Key pattern in {file}. Exposed keys enable full account compromise.",
        "suggestion": "Rotate immediately. Use IAM roles instead of static credentials. Never hardcode cloud credentials.",
        "confidence": 0.92, "tags": ["OWASP:A02", "CWE-312"],
    },
    {
        "id": "secret-generic",
        "pattern": r"(?i)(?:password|passwd|secret|api_key|apikey|auth_token|private_key|client_secret)\s*[:=]\s*['\"][^'\"]{8,}['\"]",
        "severity": "critical", "category": "secrets",
        "description": "Potential hardcoded credential in {file}: `{match}`. Secrets in source code are immediately compromised.",
        "suggestion": "Use environment variables: process.env.SECRET_NAME. For production, use a secrets manager (Vault, AWS SM, Doppler).",
        "confidence": 0.85, "tags": ["OWASP:A02", "CWE-798"],
    },
    {
        "id": "secret-github-token",
        "pattern": r"gh[pousr]_[A-Za-z0-9_]{36,255}",
        "severity": "critical", "category": "secrets",
        "description": "GitHub Personal Access Token in {file}. This provides full GitHub API access as the token owner.",
        "suggestion": "Revoke at github.com/settings/tokens immediately. Use GitHub Secrets for CI/CD and GITHUB_TOKEN for actions.",
        "confidence": 0.97, "tags": ["OWASP:A02", "CWE-312"],
    },
    {
        "id": "secret-slack-token",
        "pattern": r"xox[baprs]-[0-9]{10,12}-[0-9]{10,12}-[a-zA-Z0-9]{24,32}",
        "severity": "critical", "category": "secrets",
        "description": "Slack API token in {file}. Exposed tokens allow reading all Slack messages.",
        "suggestion": "Revoke at api.slack.com/apps immediately. Store in environment variables, never in code.",
        "confidence": 0.96, "tags": ["OWASP:A02", "CWE-312"],
    },
    {
        "id": "secret-stripe-key",
        "pattern": r"(?:sk|rk)_(?:live|test)_[0-9a-zA-Z]{24,}",
        "severity": "critical", "category": "secrets",
        "description": "Stripe API secret key in {file}. Live keys enable unauthorized charges.",
        "suggestion": "Rotate at dashboard.stripe.com/apikeys. Use restricted keys with minimal permissions.",
        "confidence": 0.97, "tags": ["OWASP:A02", "CWE-312"],
    },
    {
        "id": "secret-jwt-hardcoded",
        "pattern": r"(?i)(?:jwt.?secret|token.?secret|signing.?key)\s*[:=]\s*['\"][^'\"]{8,}['\"]",
        "severity": "critical", "category": "secrets",
        "description": "JWT signing secret hardcoded in {file}. Anyone with source access can forge any JWT.",
        "suggestion": "Generate a random 256-bit secret: openssl rand -base64 32. Store in environment variable JWT_SECRET.",
        "confidence": 0.90, "tags": ["OWASP:A02", "CWE-321"],
    },
    {
        "id": "secret-connection-string",
        "pattern": r"(?i)(?:mongodb|postgres|mysql|redis|amqp|mssql)://[^:]+:[^@]+@[^\s\"']+",
        "severity": "critical", "category": "secrets",
        "description": "Database connection string with credentials in {file}. Full database access exposed.",
        "suggestion": "Move to environment variable: DATABASE_URL=... in .env. Add .env to .gitignore.",
        "confidence": 0.92, "tags": ["OWASP:A02", "CWE-312"],
    },

    # ── Injection ─────────────────────────────────────────────────────────────
    {
        "id": "injection-sql-template",
        "pattern": r"(?i)(?:query|execute|db\.run|knex\.raw)\s*\(\s*[`\"'].*\$\{",
        "severity": "critical", "category": "injection",
        "description": "SQL injection vulnerability in {file}: user-controlled template literal in SQL query.",
        "suggestion": "Use parameterized queries: db.query('SELECT * FROM users WHERE id = $1', [id]). Or use an ORM (Prisma, Drizzle).",
        "confidence": 0.88, "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "injection-nosql",
        "pattern": r"(?i)(?:find|findOne|where)\s*\(\s*\{\s*[^}]*\$(?:where|regex|gt|lt|ne|in)\s*:",
        "severity": "high", "category": "injection",
        "description": "MongoDB query operator in {file} — potential NoSQL injection if user-controlled.",
        "suggestion": "Validate and sanitize user input before using in MongoDB queries. Use a whitelist of allowed operators.",
        "confidence": 0.75, "tags": ["OWASP:A03", "CWE-943"],
    },
    {
        "id": "injection-command",
        "pattern": r"(?:exec|execSync|spawn|spawnSync|shell\.exec)\s*\([^)]*(?:req\.|params\.|query\.|body\.|\$\{)",
        "severity": "critical", "category": "injection",
        "description": "Command injection in {file}: user-controlled input passed to shell execution.",
        "suggestion": "Never pass user input to shell commands. If unavoidable, use execFile() with arguments array (no shell interpolation).",
        "confidence": 0.85, "tags": ["OWASP:A03", "CWE-78"],
    },
    {
        "id": "injection-xss-dangeroushtml",
        "pattern": r"dangerouslySetInnerHTML\s*=\s*\{\s*\{",
        "severity": "high", "category": "injection",
        "description": "dangerouslySetInnerHTML in {file} — XSS risk if content is not sanitized.",
        "suggestion": "Sanitize with DOMPurify: { __html: DOMPurify.sanitize(content) }. Or use a safe rendering component.",
        "confidence": 0.90, "tags": ["OWASP:A03", "CWE-79"],
    },
    {
        "id": "injection-eval",
        "pattern": r"\beval\s*\(",
        "severity": "critical", "category": "injection",
        "description": "`eval()` in {file} — executes arbitrary code, enabling code injection attacks.",
        "suggestion": "Replace eval(): for JSON use JSON.parse(), for expressions use a proper expression parser (mathjs, expr-eval).",
        "confidence": 0.92, "tags": ["OWASP:A03", "CWE-95"],
    },
    {
        "id": "injection-new-function",
        "pattern": r"new\s+Function\s*\(",
        "severity": "high", "category": "injection",
        "description": "`new Function()` in {file} — similar to eval, executes arbitrary strings as code.",
        "suggestion": "Avoid dynamic code generation. Use a safe template engine or pre-compiled functions.",
        "confidence": 0.88, "tags": ["OWASP:A03", "CWE-95"],
    },
    {
        "id": "injection-ldap",
        "pattern": r"(?i)ldap(?:search|bind|modify)\s*\([^)]*(?:req\.|params\.|query\.|body\.)",
        "severity": "high", "category": "injection",
        "description": "LDAP injection risk in {file}: user input in LDAP operation.",
        "suggestion": "Escape LDAP special characters: \\, *, (, ), NUL. Use an LDAP escaping library.",
        "confidence": 0.80, "tags": ["OWASP:A03", "CWE-90"],
    },
    {
        "id": "injection-xpath",
        "pattern": r"(?i)xpath\s*\([^)]*(?:req\.|params\.|query\.|body\.|\+)",
        "severity": "high", "category": "injection",
        "description": "XPath injection risk in {file}: user input in XPath expression.",
        "suggestion": "Use parameterized XPath queries or escape user input before including in XPath.",
        "confidence": 0.78, "tags": ["OWASP:A03", "CWE-643"],
    },
    {
        "id": "injection-ssti",
        "pattern": r"(?i)(?:render|template|compile)\s*\([^)]*(?:req\.|params\.|query\.|body\.|\$\{)",
        "severity": "high", "category": "injection",
        "description": "Potential Server-Side Template Injection (SSTI) in {file}: user data in template rendering.",
        "suggestion": "Never render user-controlled data as template code. Always pass user data as template variables, not template strings.",
        "confidence": 0.72, "tags": ["OWASP:A03", "CWE-1336"],
    },

    # ── Broken Access Control ─────────────────────────────────────────────────
    {
        "id": "auth-missing-check",
        "pattern": r"(?i)req\.user\s*=|token\s*==|session\s*\[",
        "severity": "medium", "category": "auth",
        "description": "Auth-related assignment in {file} — verify access control logic is correct.",
        "suggestion": "Use a proven auth middleware. Never trust client-provided role/user data without server-side verification.",
        "confidence": 0.65, "tags": ["OWASP:A01", "CWE-862"],
    },
    {
        "id": "auth-open-redirect",
        "pattern": r"res\.redirect\s*\(\s*req\.(?:query|body|params)",
        "severity": "high", "category": "auth",
        "description": "Open redirect in {file}: redirecting to user-controlled URL enables phishing.",
        "suggestion": "Validate redirect target against an allowlist of trusted paths/domains before redirecting.",
        "confidence": 0.88, "tags": ["OWASP:A01", "CWE-601"],
    },
    {
        "id": "auth-idor",
        "pattern": r"(?i)(?:findById|findOne|getById)\s*\(\s*req\.(?:params|query|body)",
        "severity": "high", "category": "auth",
        "description": "Potential IDOR in {file}: fetching resource by user-controlled ID without ownership check.",
        "suggestion": "Always verify: `AND userId = currentUser.id` in the query. Never trust client-provided IDs alone.",
        "confidence": 0.78, "tags": ["OWASP:A01", "CWE-639"],
    },
    {
        "id": "auth-jwt-no-verify",
        "pattern": r"jwt\.decode\s*\(",
        "severity": "high", "category": "auth",
        "description": "`jwt.decode()` in {file} does NOT verify the signature — any forged JWT will be accepted.",
        "suggestion": "Use `jwt.verify(token, secret, options)`. Always verify before trusting JWT claims.",
        "confidence": 0.95, "tags": ["OWASP:A07", "CWE-347"],
    },
    {
        "id": "auth-weak-password-hash",
        "pattern": r"(?i)(?:md5|sha1|sha256)\s*\([^)]*password",
        "severity": "high", "category": "crypto",
        "description": "Weak password hashing in {file}: MD5/SHA1/SHA256 are not password hash functions.",
        "suggestion": "Use bcrypt, argon2, or scrypt: bcrypt.hash(password, 12). These are specifically designed to be slow.",
        "confidence": 0.85, "tags": ["OWASP:A02", "CWE-916"],
    },

    # ── Cryptography Failures ─────────────────────────────────────────────────
    {
        "id": "crypto-insecure-random",
        "pattern": r"\bMath\.random\s*\(",
        "severity": "medium", "category": "crypto",
        "description": "Math.random() in {file} is not cryptographically secure — predictable for security purposes.",
        "suggestion": "Use crypto.randomUUID() for IDs, crypto.randomBytes(n) for tokens, crypto.getRandomValues() in browsers.",
        "confidence": 0.90, "tags": ["OWASP:A02", "CWE-330"],
    },
    {
        "id": "crypto-hardcoded-iv",
        "pattern": r"(?i)(?:iv|initialization_vector)\s*[:=]\s*['\"][0-9a-f]{16,32}['\"]",
        "severity": "high", "category": "crypto",
        "description": "Hardcoded initialization vector in {file}. Reusing IVs completely breaks cipher security.",
        "suggestion": "Generate a random IV for each encryption: const iv = crypto.randomBytes(16). Store IV alongside the ciphertext.",
        "confidence": 0.82, "tags": ["OWASP:A02", "CWE-329"],
    },
    {
        "id": "crypto-ecb-mode",
        "pattern": r"(?i)aes.{0,10}ecb|createCipher\s*\(\s*['\"]aes",
        "severity": "high", "category": "crypto",
        "description": "ECB cipher mode or deprecated createCipher in {file}. ECB doesn't hide data patterns.",
        "suggestion": "Use AES-256-GCM (authenticated encryption): createCipheriv('aes-256-gcm', key, iv). Never use ECB.",
        "confidence": 0.85, "tags": ["OWASP:A02", "CWE-327"],
    },
    {
        "id": "crypto-weak-tls",
        "pattern": r"(?i)ssl_v[23]|tls_v1_[01]|rejectUnauthorized\s*:\s*false",
        "severity": "critical", "category": "crypto",
        "description": "Weak TLS configuration in {file}: deprecated protocol or certificate validation disabled.",
        "suggestion": "Use TLS 1.2+ only. Never set rejectUnauthorized: false in production — it completely disables certificate validation.",
        "confidence": 0.88, "tags": ["OWASP:A02", "CWE-295"],
    },

    # ── Security Misconfiguration ─────────────────────────────────────────────
    {
        "id": "config-debug-true",
        "pattern": r"(?i)(?:debug|debugMode)\s*[:=]\s*true",
        "severity": "medium", "category": "config",
        "description": "Debug mode enabled in {file} — may expose stack traces, verbose errors, or internal state in production.",
        "suggestion": "Gate debug mode on NODE_ENV: debug: process.env.NODE_ENV !== 'production'. Never enable in production.",
        "confidence": 0.80, "tags": ["OWASP:A05", "CWE-489"],
    },
    {
        "id": "config-cors-wildcard",
        "pattern": r"(?i)(?:cors|Access-Control-Allow-Origin)\s*[:=].*['\*'\"]\s*['\"]?\*['\"]?",
        "severity": "medium", "category": "config",
        "description": "CORS wildcard (*) in {file} allows any origin to make cross-origin requests.",
        "suggestion": "Specify exact allowed origins: cors({ origin: ['https://app.yourdomain.com'] }). Use env var for flexibility.",
        "confidence": 0.80, "tags": ["OWASP:A05", "CWE-942"],
    },
    {
        "id": "config-sensitive-log",
        "pattern": r"(?i)console\.(?:log|info|debug)\s*\([^)]*(?:password|token|secret|key|auth|credential)",
        "severity": "high", "category": "config",
        "description": "Sensitive data logged to console in {file}. Log aggregators often collect this.",
        "suggestion": "Redact before logging: logger.info({ user: user.id }). Never log passwords, tokens, or secrets.",
        "confidence": 0.85, "tags": ["OWASP:A09", "CWE-532"],
    },

    # ── Path Traversal / SSRF ─────────────────────────────────────────────────
    {
        "id": "path-traversal",
        "pattern": r"(?:readFile|writeFile|createReadStream|join)\s*\([^)]*(?:req\.|params\.|query\.|body\.)",
        "severity": "high", "category": "security",
        "description": "Potential path traversal in {file}: user-controlled input in file system operation.",
        "suggestion": "Validate path: const safe = path.resolve(baseDir, userPath); if (!safe.startsWith(baseDir)) throw new Error('...').",
        "confidence": 0.78, "tags": ["OWASP:A01", "CWE-22"],
    },
    {
        "id": "ssrf",
        "pattern": r"(?:fetch|axios\.get|http\.get|https\.get|got)\s*\(\s*(?:req\.|params\.|query\.|body\.|url\b)",
        "severity": "high", "category": "security",
        "description": "SSRF risk in {file}: user-controlled URL in outbound HTTP request.",
        "suggestion": "Validate URL against allowlist of trusted domains. Block internal IPs (169.254.x.x, 10.x.x.x, 172.16-31.x.x, 192.168.x.x).",
        "confidence": 0.72, "tags": ["OWASP:A10", "CWE-918"],
    },

    # ── Prototype / Supply Chain ──────────────────────────────────────────────
    {
        "id": "prototype-pollution",
        "pattern": r"\.__proto__\s*=|Object\.prototype\[|constructor\.prototype\s*=",
        "severity": "high", "category": "security",
        "description": "Prototype mutation in {file}. Modifying Object.prototype enables prototype pollution attacks.",
        "suggestion": "Use Object.create(null) for pure data stores. Validate input keys: if (key === '__proto__') return.",
        "confidence": 0.88, "tags": ["OWASP:A08", "CWE-1321"],
    },
    {
        "id": "deserialization",
        "pattern": r"(?i)(?:unserialize|pickle\.loads|yaml\.load\b|eval\(JSON)",
        "severity": "high", "category": "security",
        "description": "Unsafe deserialization in {file}. Deserializing untrusted data can execute arbitrary code.",
        "suggestion": "Use safe deserializers: yaml.safeLoad() (deprecated) → yaml.load(str, { schema: SAFE_SCHEMA }). Validate schema before deserialization.",
        "confidence": 0.82, "tags": ["OWASP:A08", "CWE-502"],
    },
    {
        "id": "xxe",
        "pattern": r"(?:libxmljs|DOMParser|parseFromString|xml2js\.parse|XMLParser)",
        "severity": "medium", "category": "security",
        "description": "XML parsing in {file} — verify XXE protection is enabled.",
        "suggestion": "Disable external entity processing. For xml2js: { explicitArray: false }. For DOMParser always validate input.",
        "confidence": 0.70, "tags": ["OWASP:A05", "CWE-611"],
    },
    {
        "id": "regex-dos",
        "pattern": r"new\s+RegExp\s*\(\s*(?:req\.|params\.|query\.|body\.|\$\{)",
        "severity": "high", "category": "security",
        "description": "ReDoS risk in {file}: user-controlled RegExp. Crafted input can freeze the event loop for minutes.",
        "suggestion": "Never construct RegExp from user input. If needed, use a safe regex library with timeout support.",
        "confidence": 0.82, "tags": ["OWASP:A05", "CWE-1333"],
    },

    # ── Race Conditions ───────────────────────────────────────────────────────
    {
        "id": "toctou",
        "pattern": r"(?:existsSync|access\b|stat\b)\s*\([^)]+\)[^;]*(?:readFile|writeFile|unlink)",
        "severity": "medium", "category": "security",
        "description": "TOCTOU race condition in {file}: check-then-use file access pattern.",
        "suggestion": "Use atomic operations. Open the file directly and handle ENOENT/EACCES exceptions rather than checking first.",
        "confidence": 0.72, "tags": ["CWE-362"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # PYTHON-SPECIFIC PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "py-pickle-unsafe",
        "pattern": r"pickle\.loads?\s*\(",
        "severity": "critical", "category": "deserialization",
        "description": "pickle.load/loads in {file} deserializes arbitrary Python objects — code execution if input is untrusted.",
        "suggestion": "Never unpickle untrusted data. Use json, msgpack, or protobuf. If you must, use hmac to sign payloads.",
        "confidence": 0.92, "tags": ["CWE-502", "OWASP:A08"],
    },
    {
        "id": "py-yaml-unsafe",
        "pattern": r"yaml\.load\s*\(",
        "severity": "high", "category": "deserialization",
        "description": "yaml.load() in {file} executes arbitrary Python via !!python/object. Use yaml.safe_load().",
        "suggestion": "Replace: yaml.safe_load(data). Never use yaml.load() with untrusted input (CWE-502).",
        "confidence": 0.93, "tags": ["CWE-502", "OWASP:A08"],
    },
    {
        "id": "py-subprocess-shell",
        "pattern": r"subprocess\.\w+\s*\([^)]*shell\s*=\s*True",
        "severity": "critical", "category": "injection",
        "description": "subprocess with shell=True in {file}: enables shell injection if any argument is user-controlled.",
        "suggestion": "Use shell=False (default) and pass args as a list: subprocess.run(['cmd', arg1, arg2]). Never interpolate user input.",
        "confidence": 0.88, "tags": ["CWE-78", "OWASP:A03"],
    },
    {
        "id": "py-os-system",
        "pattern": r"os\.(?:system|popen)\s*\(",
        "severity": "high", "category": "injection",
        "description": "os.system/popen in {file} — shell injection risk if any user data is included.",
        "suggestion": "Use subprocess.run(['cmd', arg]) with shell=False. os.system/popen both invoke a shell.",
        "confidence": 0.82, "tags": ["CWE-78", "OWASP:A03"],
    },
    {
        "id": "py-eval",
        "pattern": r"\beval\s*\(",
        "severity": "critical", "category": "injection",
        "description": "eval() in {file} executes arbitrary Python code — code injection if input is user-controlled.",
        "suggestion": "Remove eval(). For math: use ast.literal_eval() or the `numexpr` library. For config: use json/yaml.",
        "confidence": 0.90, "tags": ["CWE-95", "OWASP:A03"],
    },
    {
        "id": "py-exec",
        "pattern": r"\bexec\s*\(",
        "severity": "critical", "category": "injection",
        "description": "exec() in {file} executes arbitrary Python — same risk profile as eval().",
        "suggestion": "Remove exec(). If code generation is needed, use restricted AST evaluation or a sandboxed subprocess.",
        "confidence": 0.88, "tags": ["CWE-95", "OWASP:A03"],
    },
    {
        "id": "py-assert-security",
        "pattern": r"assert\s+.{0,60}(?:auth|permission|role|admin|user|token)",
        "severity": "high", "category": "auth",
        "description": "Security check via assert() in {file}. Python assert is disabled with -O flag — authorization bypassed in optimized mode.",
        "suggestion": "Use explicit: if not condition: raise PermissionError('Access denied'). Never use assert for security checks.",
        "confidence": 0.80, "tags": ["CWE-617", "OWASP:A01"],
    },
    {
        "id": "py-jinja2-autoescape-off",
        "pattern": r"jinja2\.Environment\s*\([^)]*autoescape\s*=\s*False",
        "severity": "high", "category": "injection",
        "description": "Jinja2 autoescape=False in {file} — XSS vulnerability if user data rendered in templates.",
        "suggestion": "Use autoescape=True (default for HTML templates). Use | e filter or select_autoescape(['html', 'xml']).",
        "confidence": 0.90, "tags": ["CWE-79", "OWASP:A03"],
    },
    {
        "id": "py-sql-concat",
        "pattern": r"(?:execute|cursor\.execute)\s*\([^)]*(?:\+|%\s*[^']|\.format\s*\(|f['\"])",
        "severity": "critical", "category": "injection",
        "description": "SQL injection in {file}: string concatenation or f-string in SQL query.",
        "suggestion": "Use parameterized queries: cursor.execute('SELECT * FROM t WHERE id = %s', (user_id,)). Never interpolate user data.",
        "confidence": 0.85, "tags": ["CWE-89", "OWASP:A03"],
    },
    {
        "id": "py-tempfile-insecure",
        "pattern": r"(?:tempfile\.mktemp|open\s*\(['\"]\/tmp\/)",
        "severity": "medium", "category": "security",
        "description": "Insecure temp file in {file}: tempfile.mktemp() or /tmp/ with predictable names is vulnerable to symlink attacks.",
        "suggestion": "Use tempfile.mkstemp() or tempfile.TemporaryFile() — these create files atomically with random names.",
        "confidence": 0.80, "tags": ["CWE-377"],
    },
    {
        "id": "py-hash-md5-sha1-password",
        "pattern": r"hashlib\.(?:md5|sha1)\s*\([^)]*(?:password|passwd|pwd|secret)",
        "severity": "high", "category": "crypto",
        "description": "Weak password hash (MD5/SHA1) in {file}. These are fast hashes, not password hash functions.",
        "suggestion": "Use bcrypt, argon2-cffi, or hashlib.scrypt(): bcrypt.hashpw(password.encode(), bcrypt.gensalt(rounds=12)).",
        "confidence": 0.90, "tags": ["CWE-916", "OWASP:A02"],
    },
    {
        "id": "py-flask-debug",
        "pattern": r"app\.run\s*\([^)]*debug\s*=\s*True",
        "severity": "critical", "category": "config",
        "description": "Flask debug=True in {file}. The interactive debugger allows arbitrary code execution via the browser.",
        "suggestion": "Remove debug=True from production. Use: app.run(debug=os.environ.get('FLASK_DEBUG') == '1').",
        "confidence": 0.95, "tags": ["CWE-94", "OWASP:A05"],
    },
    {
        "id": "py-django-secret-key",
        "pattern": r"SECRET_KEY\s*=\s*['\"][^$][^'\"]{10,}['\"]",
        "severity": "critical", "category": "secrets",
        "description": "Django SECRET_KEY hardcoded in {file}. Used for signing sessions, CSRF tokens, and cookies.",
        "suggestion": "Use: SECRET_KEY = os.environ['DJANGO_SECRET_KEY']. Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'",
        "confidence": 0.88, "tags": ["CWE-312", "OWASP:A02"],
    },
    {
        "id": "py-django-debug-true",
        "pattern": r"DEBUG\s*=\s*True",
        "severity": "high", "category": "config",
        "description": "Django DEBUG=True in {file} exposes full stack traces, SQL queries, and settings in error pages.",
        "suggestion": "Set DEBUG = os.getenv('DEBUG', 'False') == 'True'. Never deploy with DEBUG=True.",
        "confidence": 0.85, "tags": ["CWE-215", "OWASP:A05"],
    },
    {
        "id": "py-requests-no-verify",
        "pattern": r"requests\.\w+\s*\([^)]*verify\s*=\s*False",
        "severity": "high", "category": "crypto",
        "description": "requests verify=False in {file} disables TLS certificate validation — MITM attacks possible.",
        "suggestion": "Remove verify=False. If using self-signed certs, use verify='/path/to/ca-bundle.pem' instead.",
        "confidence": 0.92, "tags": ["CWE-295", "OWASP:A02"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # GO-SPECIFIC PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "go-sql-sprintf",
        "pattern": r"fmt\.Sprintf\s*\([^)]*(?:SELECT|INSERT|UPDATE|DELETE|WHERE)",
        "severity": "critical", "category": "injection",
        "description": "SQL injection via fmt.Sprintf in {file}: string-formatted SQL query.",
        "suggestion": "Use parameterized queries: db.Query('SELECT * FROM users WHERE id = $1', userID). Never fmt.Sprintf into SQL.",
        "confidence": 0.87, "tags": ["CWE-89", "OWASP:A03"],
    },
    {
        "id": "go-exec-user-input",
        "pattern": r"exec\.Command\s*\([^)]*(?:r\.|req\.|query\.|body\.)",
        "severity": "critical", "category": "injection",
        "description": "Command injection in {file}: user-controlled input in exec.Command().",
        "suggestion": "Validate all inputs strictly. Use exec.CommandContext with a timeout. Never pass user data as command arguments.",
        "confidence": 0.82, "tags": ["CWE-78", "OWASP:A03"],
    },
    {
        "id": "go-tls-skip-verify",
        "pattern": r"InsecureSkipVerify\s*:\s*true",
        "severity": "critical", "category": "crypto",
        "description": "TLS InsecureSkipVerify=true in {file} — certificate validation disabled, MITM attacks possible.",
        "suggestion": "Remove InsecureSkipVerify. For custom CAs: load the cert pool with x509.NewCertPool() and tls.Config{RootCAs: pool}.",
        "confidence": 0.97, "tags": ["CWE-295", "OWASP:A02"],
    },
    {
        "id": "go-rand-not-crypto",
        "pattern": r"math/rand|rand\.(?:Int|Float|Seed)\b",
        "severity": "medium", "category": "crypto",
        "description": "math/rand in {file} is not cryptographically secure — predictable for security contexts.",
        "suggestion": "Use crypto/rand for security-sensitive randomness: rand.Read(b). For UUIDs: github.com/google/uuid.",
        "confidence": 0.80, "tags": ["CWE-330", "OWASP:A02"],
    },
    {
        "id": "go-path-traversal",
        "pattern": r"filepath\.Join\s*\([^)]*(?:r\.|req\.|query\.|Param|Query)\s*\(",
        "severity": "high", "category": "security",
        "description": "Path traversal risk in {file}: user input in filepath.Join().",
        "suggestion": "Use filepath.Clean() and verify result starts with the expected base: filepath.Abs + strings.HasPrefix.",
        "confidence": 0.78, "tags": ["CWE-22", "OWASP:A01"],
    },
    {
        "id": "go-goroutine-leak",
        "pattern": r"go\s+func\s*\(",
        "severity": "low", "category": "performance",
        "description": "Goroutine launched in {file} — verify it has a shutdown path (context.Done, channel close) to prevent leaks.",
        "suggestion": "Always provide a cancellation mechanism: select { case <-ctx.Done(): return }. Use errgroup or sync.WaitGroup.",
        "confidence": 0.60, "tags": ["CWE-400"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # JAVA / SPRING-SPECIFIC PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "java-sql-concat",
        "pattern": r"(?:createQuery|createNativeQuery|executeQuery|prepareStatement)\s*\([^)]*\+",
        "severity": "critical", "category": "injection",
        "description": "SQL injection via string concatenation in {file}.",
        "suggestion": "Use PreparedStatement with ? placeholders, or Spring Data JPA with @Query('... WHERE id = :id') parameters.",
        "confidence": 0.85, "tags": ["CWE-89", "OWASP:A03"],
    },
    {
        "id": "java-log4j-jndi",
        "pattern": r"\$\{jndi:",
        "severity": "critical", "category": "injection",
        "description": "Log4Shell payload pattern in {file} (CVE-2021-44228). If logged via Log4j < 2.15, enables RCE.",
        "suggestion": "Upgrade Log4j >= 2.17.1. Set: log4j2.formatMsgNoLookups=true. Filter JNDI lookup strings from all user inputs.",
        "confidence": 0.98, "cve_id": "CVE-2021-44228", "tags": ["CWE-502", "OWASP:A08"],
    },
    {
        "id": "java-spring-actuator-open",
        "pattern": r"management\.endpoints\.web\.exposure\.include\s*=\s*\*",
        "severity": "high", "category": "config",
        "description": "Spring Boot Actuator exposes ALL endpoints in {file} — /actuator/env, /actuator/shutdown publicly accessible.",
        "suggestion": "Expose only: management.endpoints.web.exposure.include=health,info. Require auth: management.endpoint.health.show-details=when-authorized.",
        "confidence": 0.92, "tags": ["CWE-200", "OWASP:A05"],
    },
    {
        "id": "java-deserialize-readobject",
        "pattern": r"(?:ObjectInputStream|readObject)\s*\(",
        "severity": "high", "category": "deserialization",
        "description": "Java deserialization in {file} via ObjectInputStream. Gadget chains can achieve RCE with crafted payloads.",
        "suggestion": "Use JSON (Jackson with type polymorphism disabled), Protobuf, or Avro instead. If you must deserialize: use a filtering ObjectInputStream.",
        "confidence": 0.82, "tags": ["CWE-502", "OWASP:A08"],
    },
    {
        "id": "java-xxe",
        "pattern": r"DocumentBuilderFactory\.newInstance\s*\(",
        "severity": "high", "category": "security",
        "description": "XML parsing in {file} — verify XXE protection: DocumentBuilderFactory must disable external entities.",
        "suggestion": "factory.setFeature('http://apache.org/xml/features/disallow-doctype-decl', true). Always harden XML parsers.",
        "confidence": 0.75, "tags": ["CWE-611", "OWASP:A05"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # PHP-SPECIFIC PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "php-sql-concat",
        "pattern": r"(?:mysql_query|mysqli_query|pg_query)\s*\([^)]*(?:\$_GET|\$_POST|\$_REQUEST|\$_COOKIE)",
        "severity": "critical", "category": "injection",
        "description": "SQL injection in {file}: user superglobal directly in SQL query.",
        "suggestion": "Use PDO with prepared statements: $stmt = $pdo->prepare('SELECT * FROM t WHERE id = ?'); $stmt->execute([$id]);",
        "confidence": 0.90, "tags": ["CWE-89", "OWASP:A03"],
    },
    {
        "id": "php-eval",
        "pattern": r"\beval\s*\(",
        "severity": "critical", "category": "injection",
        "description": "eval() in {file} — executes arbitrary PHP. Attackers who control input achieve full code execution.",
        "suggestion": "Remove eval(). For templating use Twig/Blade. For dynamic config use JSON.",
        "confidence": 0.92, "tags": ["CWE-95", "OWASP:A03"],
    },
    {
        "id": "php-file-include",
        "pattern": r"(?:include|require)(?:_once)?\s*\(\s*\$(?:_GET|_POST|_REQUEST|_COOKIE)",
        "severity": "critical", "category": "injection",
        "description": "Remote/local file inclusion in {file}: user input controls include path.",
        "suggestion": "Never use user input for file paths. Use an allowlist: $allowed = ['home', 'about']; include($allowed[$page] ?? 'home') . '.php';",
        "confidence": 0.95, "tags": ["CWE-98", "OWASP:A01"],
    },
    {
        "id": "php-xss-echo",
        "pattern": r"echo\s+\$_(?:GET|POST|REQUEST|COOKIE)",
        "severity": "high", "category": "injection",
        "description": "Reflected XSS in {file}: unescaped user input echoed directly to output.",
        "suggestion": "Always escape: echo htmlspecialchars($_GET['q'], ENT_QUOTES, 'UTF-8');",
        "confidence": 0.92, "tags": ["CWE-79", "OWASP:A03"],
    },
    {
        "id": "php-md5-password",
        "pattern": r"md5\s*\([^)]*(?:password|passwd|pwd)",
        "severity": "high", "category": "crypto",
        "description": "MD5 password hashing in {file} — completely insecure, crackable in seconds.",
        "suggestion": "Use PHP's built-in: password_hash($password, PASSWORD_ARGON2ID). Verify with password_verify().",
        "confidence": 0.93, "tags": ["CWE-916", "OWASP:A02"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # RUBY / RAILS-SPECIFIC PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "ruby-sql-where-string",
        "pattern": r"\.where\s*\(\s*['\"].*#\{",
        "severity": "critical", "category": "injection",
        "description": "SQL injection in {file}: string interpolation in ActiveRecord .where().",
        "suggestion": "Use parameterized form: .where('column = ?', user_input) or .where(column: user_input). Never interpolate.",
        "confidence": 0.90, "tags": ["CWE-89", "OWASP:A03"],
    },
    {
        "id": "ruby-eval",
        "pattern": r"\beval\s*\(",
        "severity": "critical", "category": "injection",
        "description": "eval() in {file} — executes arbitrary Ruby code.",
        "suggestion": "Remove eval(). Use Ripper for parsing, Binding#eval only with fully-trusted inputs.",
        "confidence": 0.88, "tags": ["CWE-95", "OWASP:A03"],
    },
    {
        "id": "ruby-send-user-input",
        "pattern": r"\.send\s*\(\s*params",
        "severity": "high", "category": "auth",
        "description": "Ruby Object#send with user params in {file} — can call any method including private ones.",
        "suggestion": "Use an allowlist: ALLOWED = %w[activate deactivate]; obj.send(ALLOWED.find { |m| m == params[:action] })",
        "confidence": 0.85, "tags": ["CWE-284", "OWASP:A01"],
    },
    {
        "id": "ruby-mass-assignment",
        "pattern": r"update_attributes\s*\(\s*params|create\s*\(\s*params(?!\[)",
        "severity": "high", "category": "auth",
        "description": "Rails mass assignment with unfiltered params in {file} — IDOR and privilege escalation risk.",
        "suggestion": "Always use strong parameters: params.require(:user).permit(:name, :email). Never pass params directly.",
        "confidence": 0.82, "tags": ["CWE-915", "OWASP:A01"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # C / C++ PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "c-strcpy-unsafe",
        "pattern": r"\bstrcpy\s*\(",
        "severity": "high", "category": "security",
        "description": "strcpy() in {file} has no bounds checking — buffer overflow if source > destination.",
        "suggestion": "Use strncpy(dest, src, sizeof(dest) - 1) or strlcpy(). Better: use C++ std::string or snprintf.",
        "confidence": 0.88, "tags": ["CWE-120", "CWE-787"],
    },
    {
        "id": "c-gets-unsafe",
        "pattern": r"\bgets\s*\(",
        "severity": "critical", "category": "security",
        "description": "gets() in {file} — always unsafe (removed in C11). Allows unlimited buffer overflow.",
        "suggestion": "Replace with fgets(buf, sizeof(buf), stdin). gets() has no buffer size argument and cannot be made safe.",
        "confidence": 0.97, "tags": ["CWE-120", "CWE-242"],
    },
    {
        "id": "c-sprintf-unsafe",
        "pattern": r"\bsprintf\s*\(",
        "severity": "medium", "category": "security",
        "description": "sprintf() in {file} — unbounded output can overflow the destination buffer.",
        "suggestion": "Use snprintf(buf, sizeof(buf), fmt, args) — always specify the maximum size.",
        "confidence": 0.82, "tags": ["CWE-120"],
    },
    {
        "id": "c-printf-format-string",
        "pattern": r"printf\s*\(\s*(?:argv|getenv|user_input|buf)\b",
        "severity": "critical", "category": "injection",
        "description": "Format string injection in {file}: user-controlled first argument to printf().",
        "suggestion": "Always use a format string literal: printf('%s', user_input). Never printf(user_input).",
        "confidence": 0.90, "tags": ["CWE-134"],
    },
    {
        "id": "c-malloc-no-check",
        "pattern": r"(?:malloc|calloc|realloc)\s*\([^;]+;\s*[^i]",
        "severity": "low", "category": "quality",
        "description": "malloc/calloc/realloc result unchecked in {file} — NULL dereference on allocation failure.",
        "suggestion": "Always check: ptr = malloc(n); if (!ptr) { /* handle OOM */ }. Or use a wrapper that aborts on failure.",
        "confidence": 0.65, "tags": ["CWE-476"],
    },
    {
        "id": "c-integer-overflow",
        "pattern": r"(?:malloc|calloc)\s*\(\s*(?:\w+\s*\*\s*\w+|\w+\s*\+\s*\w+)",
        "severity": "high", "category": "security",
        "description": "Integer arithmetic in allocation size in {file} — integer overflow can lead to under-allocation and heap overflow.",
        "suggestion": "Use checked arithmetic: if (n > SIZE_MAX / sizeof(T)) abort(); Or use calloc(count, sizeof(T)) which handles overflow.",
        "confidence": 0.72, "tags": ["CWE-190", "CWE-787"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # DOCKER / CONTAINER / INFRASTRUCTURE PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "docker-root-user",
        "pattern": r"USER\s+root",
        "severity": "high", "category": "config",
        "description": "Container runs as root in {file}. Container escape gives full host access.",
        "suggestion": "Add: RUN adduser --disabled-password appuser && chown -R appuser:appuser /app\nUSER appuser",
        "confidence": 0.88, "tags": ["CWE-250", "OWASP:A05"],
    },
    {
        "id": "docker-no-user",
        "pattern": r"^FROM\b(?!.*USER\s+\w)",
        "severity": "medium", "category": "config",
        "description": "Dockerfile in {file} may not set a non-root USER — container defaults to root.",
        "suggestion": "Always add USER nonroot before the final CMD/ENTRYPOINT.",
        "confidence": 0.60, "tags": ["CWE-250"],
    },
    {
        "id": "docker-secret-build-arg",
        "pattern": r"ARG\s+(?:password|secret|token|key|api)",
        "severity": "high", "category": "secrets",
        "description": "Secret passed as Docker build ARG in {file} — ARGs are stored in image layers and history.",
        "suggestion": "Use Docker secrets (--secret mount) or multi-stage builds. Never bake secrets into image layers.",
        "confidence": 0.85, "tags": ["CWE-312", "OWASP:A02"],
    },
    {
        "id": "terraform-public-s3",
        "pattern": r"acl\s*=\s*['\"]public-read",
        "severity": "high", "category": "config",
        "description": "S3 bucket public-read ACL in {file} — bucket contents publicly accessible to anyone.",
        "suggestion": "Use private ACL and CloudFront + OAI for public content distribution. Enable S3 Block Public Access at account level.",
        "confidence": 0.90, "tags": ["CWE-200", "OWASP:A05"],
    },
    {
        "id": "terraform-sg-open-world",
        "pattern": r"cidr_blocks\s*=\s*\[\"0\.0\.0\.0/0\"\]",
        "severity": "high", "category": "config",
        "description": "Security group open to 0.0.0.0/0 in {file} — port exposed to entire internet.",
        "suggestion": "Restrict to: known IP ranges, VPC CIDR, or use a load balancer. Never expose databases (3306, 5432, 6379) to 0.0.0.0/0.",
        "confidence": 0.88, "tags": ["CWE-732", "OWASP:A05"],
    },
    {
        "id": "k8s-privileged-container",
        "pattern": r"privileged\s*:\s*true",
        "severity": "critical", "category": "config",
        "description": "Kubernetes privileged container in {file} — equivalent to root on the host node.",
        "suggestion": "Remove privileged: true. Use specific capabilities: capabilities.add: [NET_BIND_SERVICE]. Apply PodSecurityPolicy or OPA.",
        "confidence": 0.92, "tags": ["CWE-250", "OWASP:A05"],
    },
    {
        "id": "k8s-host-network",
        "pattern": r"hostNetwork\s*:\s*true",
        "severity": "high", "category": "config",
        "description": "hostNetwork: true in {file} — container shares the host network namespace, bypassing network isolation.",
        "suggestion": "Remove hostNetwork: true unless absolutely required (e.g., node-level monitoring). Use proper service meshes instead.",
        "confidence": 0.88, "tags": ["CWE-284", "OWASP:A05"],
    },
    {
        "id": "k8s-latest-image",
        "pattern": r"image:\s+\w[^:]+:latest",
        "severity": "medium", "category": "deps",
        "description": "Container image tagged :latest in {file} — non-deterministic builds, may pull insecure images.",
        "suggestion": "Pin to specific digest: image: nginx@sha256:abc123... Or a specific version tag: nginx:1.25.3",
        "confidence": 0.85, "tags": ["CWE-1104", "OWASP:A06"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # API SECURITY PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "api-no-rate-limit",
        "pattern": r"(?:router\.\w+|app\.(?:get|post|put|delete|patch))\s*\([^)]*['\"]\/api\/",
        "severity": "low", "category": "config",
        "description": "API route in {file} — verify rate limiting is applied to prevent brute-force and DoS.",
        "suggestion": "Apply rate limiting: express-rate-limit, or throttle at API gateway level. Login/auth endpoints need strict limits.",
        "confidence": 0.55, "tags": ["CWE-400", "OWASP:A05"],
    },
    {
        "id": "api-mass-return",
        "pattern": r"res\.json\s*\(\s*(?:users|accounts|customers|employees|passwords)\b",
        "severity": "high", "category": "auth",
        "description": "Potential bulk data exposure in {file}: entire user/account collection may be returned.",
        "suggestion": "Paginate results. Filter sensitive fields: users.map(({ id, name }) => ({ id, name })). Never return passwords/tokens.",
        "confidence": 0.70, "tags": ["CWE-213", "OWASP:A01"],
    },
    {
        "id": "api-auth-bearer-log",
        "pattern": r"(?:console\.|log\.|logger\.)\w+\s*\([^)]*(?:authorization|bearer|token)",
        "severity": "high", "category": "config",
        "description": "Authorization token logged in {file}. Tokens in logs enable session hijacking.",
        "suggestion": "Redact: logger.info({ auth: '[REDACTED]' }). Use structured logging that masks sensitive headers automatically.",
        "confidence": 0.82, "tags": ["CWE-532", "OWASP:A09"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # NETWORK / PROTOCOL PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "net-http-not-https",
        "pattern": r"['\"]http://(?!localhost|127\.0\.0\.1|0\.0\.0\.0)",
        "severity": "medium", "category": "crypto",
        "description": "Cleartext HTTP URL in {file} — data transmitted without encryption, vulnerable to MITM.",
        "suggestion": "Use https:// for all production endpoints. Set up HSTS: Strict-Transport-Security: max-age=31536000; includeSubDomains.",
        "confidence": 0.72, "tags": ["CWE-319", "OWASP:A02"],
    },
    {
        "id": "net-bind-all-interfaces",
        "pattern": r"(?:listen|bind)\s*\(['\"]0\.0\.0\.0['\"]",
        "severity": "medium", "category": "config",
        "description": "Service binds to 0.0.0.0 in {file} — accessible on ALL network interfaces including public ones.",
        "suggestion": "Bind to 127.0.0.1 for local services. Use a reverse proxy (nginx/caddy) to expose externally with proper security.",
        "confidence": 0.72, "tags": ["CWE-605", "OWASP:A05"],
    },
    {
        "id": "net-websocket-origin",
        "pattern": r"WebSocket|ws(?:s)?://",
        "severity": "low", "category": "config",
        "description": "WebSocket in {file} — verify Origin header validation to prevent cross-site WebSocket hijacking (CSWSH).",
        "suggestion": "Validate Origin header on WebSocket upgrade: if (req.headers.origin !== ALLOWED_ORIGIN) socket.destroy().",
        "confidence": 0.55, "tags": ["CWE-346", "OWASP:A01"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # DATABASE PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "db-prisma-raw-query",
        "pattern": r"prisma\.\$queryRaw(?:Unsafe)?\s*\(`[^`]*\$\{",
        "severity": "high", "category": "injection",
        "description": "SQL injection risk in {file}: template literal in Prisma $queryRaw.",
        "suggestion": "Use $queryRaw with tagged template literal (Prisma escapes automatically), or $queryRawUnsafe only with fully-trusted inputs.",
        "confidence": 0.85, "tags": ["CWE-89", "OWASP:A03"],
    },
    {
        "id": "db-mongoose-where-string",
        "pattern": r"\.find\s*\(\s*\{[^}]*\$where",
        "severity": "critical", "category": "injection",
        "description": "MongoDB $where operator in {file} — executes JavaScript engine, enables code injection.",
        "suggestion": "Never use $where. Rewrite using comparison operators ($eq, $gt, $in). $where is deprecated in MongoDB 4.4+.",
        "confidence": 0.92, "tags": ["CWE-943", "OWASP:A03"],
    },
    {
        "id": "db-redis-no-auth",
        "pattern": r"redis\.createClient\s*\(\s*\{[^}]*port[^}]*\}",
        "severity": "medium", "category": "config",
        "description": "Redis client without explicit auth in {file} — verify requirepass is set in redis.conf.",
        "suggestion": "Use: redis.createClient({ url: 'redis://:password@host:6379' }). Always enable Redis AUTH in production.",
        "confidence": 0.65, "tags": ["CWE-306", "OWASP:A07"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # REACT / NEXT.JS SPECIFIC
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "react-next-env-client",
        "pattern": r"NEXT_PUBLIC_(?:SECRET|KEY|TOKEN|PASSWORD|PRIVATE)",
        "severity": "critical", "category": "secrets",
        "description": "Secret exposed to client in {file}: NEXT_PUBLIC_ env vars are bundled into client JavaScript.",
        "suggestion": "Remove NEXT_PUBLIC_ prefix — only expose non-secret public data. Access secrets in server-only code (API routes, Server Components).",
        "confidence": 0.92, "tags": ["CWE-312", "OWASP:A02"],
    },
    {
        "id": "react-href-javascript",
        "pattern": r"href\s*=\s*\{?['\"]javascript:",
        "severity": "high", "category": "injection",
        "description": "javascript: protocol in href in {file} — XSS if user-controlled.",
        "suggestion": "Never use href='javascript:'. Use onClick handlers instead. Validate URLs before use: only allow http/https.",
        "confidence": 0.90, "tags": ["CWE-79", "OWASP:A03"],
    },
    {
        "id": "next-api-no-method-check",
        "pattern": r"export\s+(?:default\s+)?(?:async\s+)?function\s+handler\s*\(\s*req",
        "severity": "low", "category": "config",
        "description": "Next.js API handler in {file} — verify HTTP method is checked to prevent unwanted mutations.",
        "suggestion": "Always check: if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });",
        "confidence": 0.55, "tags": ["CWE-436", "OWASP:A05"],
    },
    {
        "id": "next-server-action-unvalidated",
        "pattern": r"['\"]use server['\"]",
        "severity": "low", "category": "auth",
        "description": "Server Action in {file} — verify input is validated and user is authenticated before execution.",
        "suggestion": "Always validate: const session = await getServerSession(); if (!session) throw new Error('Unauthorized'). Validate all inputs with Zod.",
        "confidence": 0.55, "tags": ["CWE-862", "OWASP:A01"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # CLOUD / AWS / GCP / AZURE PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "cloud-iam-star-action",
        "pattern": r"(?:\"Action\"|'Action')\s*:\s*['\"]?\*['\"]?",
        "severity": "high", "category": "config",
        "description": "IAM policy allows ALL actions (*) in {file} — violates least-privilege principle.",
        "suggestion": "Enumerate only required actions: 's3:GetObject', 's3:PutObject'. Never use Action: '*' in production.",
        "confidence": 0.85, "tags": ["CWE-732", "OWASP:A01"],
    },
    {
        "id": "cloud-iam-star-resource",
        "pattern": r"(?:\"Resource\"|'Resource')\s*:\s*['\"]?\*['\"]?",
        "severity": "high", "category": "config",
        "description": "IAM policy targets ALL resources (*) in {file} — grants access to every resource in the account.",
        "suggestion": "Specify exact ARNs: 'arn:aws:s3:::my-bucket/*'. Never use Resource: '*' for sensitive actions.",
        "confidence": 0.82, "tags": ["CWE-732", "OWASP:A01"],
    },
    {
        "id": "cloud-gcp-service-account-key",
        "pattern": r"\"type\":\s*\"service_account\"",
        "severity": "critical", "category": "secrets",
        "description": "GCP service account key file in {file}. These long-lived credentials provide full API access.",
        "suggestion": "Use Workload Identity Federation instead of service account key files. If needed, store in Secret Manager, never in code or VCS.",
        "confidence": 0.93, "tags": ["CWE-312", "OWASP:A02"],
    },
    {
        "id": "cloud-azure-connection-string",
        "pattern": r"DefaultEndpointsProtocol=https;AccountName=",
        "severity": "critical", "category": "secrets",
        "description": "Azure Storage connection string in {file} — full storage account access exposed.",
        "suggestion": "Store in Azure Key Vault or environment variables. Use Managed Identity instead of connection strings where possible.",
        "confidence": 0.97, "tags": ["CWE-312", "OWASP:A02"],
    },

    # ══════════════════════════════════════════════════════════════════════════
    # MOBILE (REACT NATIVE / FLUTTER) PATTERNS
    # ══════════════════════════════════════════════════════════════════════════
    {
        "id": "mobile-asyncstorage-secret",
        "pattern": r"AsyncStorage\.setItem\s*\([^,]+(?:token|secret|password|key)",
        "severity": "high", "category": "secrets",
        "description": "Sensitive data in AsyncStorage in {file} — not encrypted, readable by root/jailbroken devices.",
        "suggestion": "Use react-native-keychain or expo-secure-store for secrets. AsyncStorage is plain-text SQLite.",
        "confidence": 0.85, "tags": ["CWE-312", "OWASP:A02"],
    },
    {
        "id": "mobile-http-cleartext",
        "pattern": r"android:usesCleartextTraffic\s*=\s*['\"]true['\"]",
        "severity": "high", "category": "crypto",
        "description": "Cleartext HTTP allowed in Android manifest in {file} — traffic readable by MITM attackers.",
        "suggestion": "Remove usesCleartextTraffic=true. Use HTTPS everywhere. For local dev, use a proper dev certificate.",
        "confidence": 0.90, "tags": ["CWE-319", "OWASP:A02"],
    },
    {
        "id": "mobile-deeplink-unvalidated",
        "pattern": r"Linking\.getInitialURL\s*\(\s*\)",
        "severity": "medium", "category": "auth",
        "description": "Deep link URL in {file} — validate URL scheme and parameters to prevent deep link hijacking.",
        "suggestion": "Validate scheme and host: if (!url.startsWith('myapp://auth')) return. Use universal links (https://) over custom schemes.",
        "confidence": 0.72, "tags": ["CWE-601", "OWASP:A01"],
    },
]


# ─────────────────────────────────────────────────────────────────────────────
# CVE-specific patterns for known library exploits
# These are more specific and higher confidence
# ─────────────────────────────────────────────────────────────────────────────

CVE_PATTERNS: list[dict] = [
    {
        "id": "cve-2021-23337-lodash",
        "pattern": r"(?:_.template|lodash\.template)\s*\(",
        "severity": "high", "cve_id": "CVE-2021-23337",
        "category": "injection",
        "description": "lodash.template() in {file} is vulnerable to code injection (CVE-2021-23337) in versions < 4.17.21.",
        "suggestion": "Upgrade lodash >= 4.17.21. Consider replacing template literals with a dedicated safe template engine.",
        "confidence": 0.80,
    },
    {
        "id": "cve-2022-23529-jsonwebtoken",
        "pattern": r"jwt\.verify\s*\([^,]+,\s*(?:req\.|params\.|body\.)",
        "severity": "critical", "cve_id": "CVE-2022-23529",
        "category": "auth",
        "description": "jsonwebtoken.verify() called with user-controlled secret in {file} (related to CVE-2022-23529).",
        "suggestion": "The JWT secret must be a server-side constant. Never use user-provided data as the signing secret.",
        "confidence": 0.85,
    },
]

# ─────────────────────────────────────────────────────────────────────────────
# EXTENDED VULNERABILITY PATTERNS — Deep coverage across all languages
# Auto-merged into the main scan on every analysis
# ─────────────────────────────────────────────────────────────────────────────

EXTENDED_VULN_PATTERNS: list[dict] = [

    # ── Python-specific ───────────────────────────────────────────────────────
    {
        "id": "python-pickle-loads",
        "pattern": r"pickle\.loads?\s*\(",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "Insecure deserialization via pickle.loads() in {file}. Pickle can execute arbitrary code during deserialization of untrusted data.",
        "suggestion": "Never deserialize untrusted data with pickle. Use JSON, MessagePack, or protobuf instead. If pickle is required, sign the payload with HMAC before deserializing.",
        "confidence": 0.90,
        "tags": ["OWASP:A08", "CWE-502"],
    },
    {
        "id": "python-yaml-unsafe-load",
        "pattern": r"yaml\.load\s*\([^)]*\)(?!\s*#.*safe)",
        "severity": "high",
        "category": "injection",
        "language": "python",
        "description": "yaml.load() without explicit Loader in {file} can execute arbitrary Python objects embedded in YAML.",
        "suggestion": "Use yaml.safe_load() instead of yaml.load(). If full YAML loading is needed, pass Loader=yaml.SafeLoader explicitly.",
        "confidence": 0.88,
        "tags": ["OWASP:A08", "CWE-502"],
        "cve_id": "CVE-2017-18342",
    },
    {
        "id": "python-subprocess-shell-true",
        "pattern": r"subprocess\.\w+\s*\([^)]*shell\s*=\s*True",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "subprocess called with shell=True in {file}. If any part of the command includes user input, this allows arbitrary command execution.",
        "suggestion": "Use shell=False (default) and pass arguments as a list: subprocess.run(['cmd', arg1, arg2]). Never interpolate user input into shell=True commands.",
        "confidence": 0.85,
        "tags": ["OWASP:A03", "CWE-78"],
    },
    {
        "id": "python-os-system-user-input",
        "pattern": r"os\.system\s*\([^)]*(?:input|request|param|query|argv|environ)",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "os.system() called with potentially user-controlled data in {file}. This allows arbitrary OS command execution.",
        "suggestion": "Replace os.system() with subprocess.run() using a list of arguments and shell=False. Never pass user-controlled data to os.system().",
        "confidence": 0.82,
        "tags": ["OWASP:A03", "CWE-78"],
    },
    {
        "id": "python-tempfile-mktemp",
        "pattern": r"tempfile\.mktemp\s*\(",
        "severity": "medium",
        "category": "config",
        "language": "python",
        "description": "tempfile.mktemp() in {file} has a TOCTOU race condition: another process can create the file between the name generation and opening.",
        "suggestion": "Use tempfile.mkstemp() which atomically creates and opens the file, or tempfile.NamedTemporaryFile() for a context manager.",
        "confidence": 0.95,
        "tags": ["OWASP:A04", "CWE-377"],
    },
    {
        "id": "python-flask-debug-true",
        "pattern": r"app\.run\s*\([^)]*debug\s*=\s*True",
        "severity": "high",
        "category": "config",
        "language": "python",
        "description": "Flask app started with debug=True in {file}. The Werkzeug debugger exposes an interactive Python console accessible without authentication.",
        "suggestion": "Set debug=False in production. Use environment variable: app.run(debug=os.environ.get('FLASK_DEBUG', 'false').lower() == 'true'). Never enable debug in production.",
        "confidence": 0.95,
        "tags": ["OWASP:A05", "CWE-94"],
    },
    {
        "id": "python-flask-secret-key-short",
        "pattern": r"(?i)SECRET_KEY\s*=\s*['\"][^'\"]{1,20}['\"]",
        "severity": "high",
        "category": "secrets",
        "language": "python",
        "description": "Flask/Django SECRET_KEY is too short in {file}. Short keys are brute-forceable and allow session forgery.",
        "suggestion": "Generate a strong key: python -c \"import secrets; print(secrets.token_hex(32))\". Store in environment variable, never hardcode.",
        "confidence": 0.80,
        "tags": ["OWASP:A02", "CWE-321"],
    },
    {
        "id": "python-django-debug-true",
        "pattern": r"DEBUG\s*=\s*True(?!\s*#.*test)",
        "severity": "high",
        "category": "config",
        "language": "python",
        "description": "Django DEBUG=True in {file}. Debug mode exposes stack traces, local variables, and settings to end users.",
        "suggestion": "Set DEBUG = False in production. Use: DEBUG = os.environ.get('DJANGO_DEBUG', 'False') == 'True'. Keep a separate settings file for development.",
        "confidence": 0.80,
        "tags": ["OWASP:A05", "CWE-209"],
    },
    {
        "id": "python-django-allowed-hosts-wildcard",
        "pattern": r"ALLOWED_HOSTS\s*=\s*\[[\s]*['\"]?\*['\"]?[\s]*\]",
        "severity": "high",
        "category": "config",
        "language": "python",
        "description": "Django ALLOWED_HOSTS = ['*'] in {file} allows HTTP Host header injection attacks.",
        "suggestion": "Specify exact hostnames: ALLOWED_HOSTS = ['yourdomain.com', 'www.yourdomain.com']. Use environment variables to configure per-environment.",
        "confidence": 0.95,
        "tags": ["OWASP:A05", "CWE-183"],
    },
    {
        "id": "python-django-secret-key-hardcoded",
        "pattern": r"SECRET_KEY\s*=\s*['\"](?!os\.environ)[^'\"]{20,}['\"]",
        "severity": "critical",
        "category": "secrets",
        "language": "python",
        "description": "Django SECRET_KEY hardcoded in {file}. This key signs sessions, CSRF tokens, and password reset tokens — exposure compromises all users.",
        "suggestion": "Use: SECRET_KEY = os.environ['DJANGO_SECRET_KEY']. Rotate all sessions and tokens after fixing.",
        "confidence": 0.82,
        "tags": ["OWASP:A02", "CWE-321"],
    },
    {
        "id": "python-django-no-csrf-middleware",
        "pattern": r"MIDDLEWARE\s*=\s*\[(?:[^]]*\n)*[^]]*\](?![\s\S]*CsrfViewMiddleware)",
        "severity": "high",
        "category": "auth",
        "language": "python",
        "description": "Django CSRF middleware may be missing in {file}. Without CSRF protection, forged cross-site requests can perform state-changing actions.",
        "suggestion": "Ensure 'django.middleware.csrf.CsrfViewMiddleware' is present in MIDDLEWARE. Never remove it from the default list.",
        "confidence": 0.60,
        "tags": ["OWASP:A01", "CWE-352"],
    },
    {
        "id": "python-django-raw-sql-format",
        "pattern": r"\.raw\s*\(\s*['\"][^'\"]*%[s df]|\.raw\s*\(\s*f['\"]",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "Django raw() SQL query with string formatting in {file}. This is a textbook SQL injection vector.",
        "suggestion": "Use Django ORM or pass parameters: Model.objects.raw('SELECT * FROM t WHERE id = %s', [user_id]). Never use % formatting or f-strings in raw SQL.",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "python-hashlib-md5-password",
        "pattern": r"hashlib\.md5\s*\([^)]*(?:password|passwd|pwd|secret)",
        "severity": "high",
        "category": "crypto",
        "language": "python",
        "description": "MD5 used for password hashing in {file}. MD5 is cryptographically broken and trivially reversible with rainbow tables.",
        "suggestion": "Use bcrypt, argon2, or scrypt: import bcrypt; bcrypt.hashpw(password.encode(), bcrypt.gensalt()). Never use MD5/SHA1 for passwords.",
        "confidence": 0.88,
        "tags": ["OWASP:A02", "CWE-916"],
    },
    {
        "id": "python-random-for-crypto",
        "pattern": r"random\.(?:random|randint|choice|randrange)\s*\([^)]*\).*(?:token|secret|key|nonce|salt|otp|pin)",
        "severity": "high",
        "category": "crypto",
        "language": "python",
        "description": "Python random module used for cryptographic purpose near {file}. random is not cryptographically secure and is predictable.",
        "suggestion": "Use secrets module: secrets.token_hex(32), secrets.token_urlsafe(), or secrets.randbelow(). The secrets module is designed for security-sensitive use.",
        "confidence": 0.75,
        "tags": ["OWASP:A02", "CWE-338"],
    },
    {
        "id": "python-assert-security-check",
        "pattern": r"assert\s+(?:user|role|permission|auth|is_admin|is_authenticated|logged_in)",
        "severity": "high",
        "category": "auth",
        "language": "python",
        "description": "assert used for security check in {file}. Python assertions are stripped when running with -O (optimize) flag, bypassing all security checks.",
        "suggestion": "Replace assert with explicit if/raise: if not user.is_authenticated: raise PermissionError('Not authenticated'). Never rely on assert for security.",
        "confidence": 0.82,
        "tags": ["OWASP:A01", "CWE-617"],
    },
    {
        "id": "python-marshal-loads",
        "pattern": r"marshal\.loads?\s*\(",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "marshal.loads() in {file} deserializes Python bytecode. Untrusted marshal data can execute arbitrary code.",
        "suggestion": "Never deserialize marshal data from untrusted sources. Use JSON or protobuf for data exchange. marshal is for Python's internal use only.",
        "confidence": 0.92,
        "tags": ["OWASP:A08", "CWE-502"],
    },
    {
        "id": "python-exec-user-input",
        "pattern": r"exec\s*\([^)]*(?:request|input|param|query|body|argv|environ|user)",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "exec() called with potentially user-controlled data in {file}. This allows arbitrary code execution.",
        "suggestion": "Never pass user input to exec(). If dynamic evaluation is needed, use ast.literal_eval() for data, or a safe expression evaluator.",
        "confidence": 0.85,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "python-dynamic-import-user-input",
        "pattern": r"__import__\s*\([^)]*(?:request|param|query|body|input|user)",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "Dynamic __import__() with potentially user-controlled module name in {file}. Allows arbitrary module loading and code execution.",
        "suggestion": "Use a static allowlist for module names: if module_name not in ALLOWED_MODULES: raise ValueError. Never let user input control import paths.",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "python-ssl-protocol-tls1",
        "pattern": r"ssl\.PROTOCOL_TLSv1(?:_1)?\b",
        "severity": "high",
        "category": "crypto",
        "language": "python",
        "description": "Deprecated TLS 1.0/1.1 protocol in {file}. TLS 1.0 and 1.1 are vulnerable to POODLE, BEAST, and other downgrade attacks.",
        "suggestion": "Use ssl.PROTOCOL_TLS_CLIENT with ssl.SSLContext and set minimum_version=ssl.TLSVersion.TLSv1_2. Prefer TLS 1.3 where supported.",
        "confidence": 0.92,
        "tags": ["OWASP:A02", "CWE-326"],
    },
    {
        "id": "python-requests-verify-false",
        "pattern": r"requests\.\w+\s*\([^)]*verify\s*=\s*False",
        "severity": "high",
        "category": "crypto",
        "language": "python",
        "description": "TLS certificate verification disabled in requests call in {file}. This allows man-in-the-middle attacks.",
        "suggestion": "Remove verify=False. If using self-signed certs, pass the CA bundle path: verify='/path/to/ca-bundle.crt'. Never disable verification in production.",
        "confidence": 0.95,
        "tags": ["OWASP:A02", "CWE-295"],
    },
    {
        "id": "python-httpx-verify-false",
        "pattern": r"httpx\.\w+\s*\([^)]*verify\s*=\s*False",
        "severity": "high",
        "category": "crypto",
        "language": "python",
        "description": "TLS certificate verification disabled in httpx call in {file}. Susceptible to MITM attacks.",
        "suggestion": "Remove verify=False. Pass a custom CA cert path if needed: verify='/path/to/cert.pem'. Never skip TLS verification in production.",
        "confidence": 0.95,
        "tags": ["OWASP:A02", "CWE-295"],
    },
    {
        "id": "python-asyncio-no-timeout",
        "pattern": r"await\s+(?:asyncio\.gather|asyncio\.wait)\s*\([^)]*\)(?!\s*#.*timeout)",
        "severity": "medium",
        "category": "config",
        "language": "python",
        "description": "asyncio.gather()/wait() without timeout in {file}. Unbounded async operations can hang indefinitely, causing resource exhaustion.",
        "suggestion": "Wrap with asyncio.wait_for(coro, timeout=30.0) or use asyncio.wait() with timeout parameter. Always set reasonable timeouts on async I/O.",
        "confidence": 0.65,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "python-flask-no-login-required",
        "pattern": r"@app\.route\s*\([^)]+\)\s*\ndef\s+\w+\s*\([^)]*\)\s*:\s*\n(?!\s*@login_required)",
        "severity": "medium",
        "category": "auth",
        "language": "python",
        "description": "Flask route defined without @login_required decorator in {file}. Endpoint may be unintentionally public.",
        "suggestion": "Add @login_required (Flask-Login) or equivalent auth check. Use a blueprint-level before_request handler to enforce auth globally.",
        "confidence": 0.55,
        "tags": ["OWASP:A01", "CWE-862"],
    },
    {
        "id": "python-eval-user-input",
        "pattern": r"\beval\s*\([^)]*(?:request|input|param|query|body|argv|environ|user)",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "eval() called with potentially user-controlled data in {file}. Allows arbitrary Python code execution.",
        "suggestion": "Replace eval() with ast.literal_eval() for data parsing, or a safe expression library like simpleeval. Never eval untrusted input.",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "python-weak-hash-sha1-password",
        "pattern": r"hashlib\.sha1\s*\([^)]*(?:password|passwd|pwd|secret)",
        "severity": "high",
        "category": "crypto",
        "language": "python",
        "description": "SHA-1 used for password hashing in {file}. SHA-1 is not a password hashing algorithm — it is fast and brute-forceable.",
        "suggestion": "Use bcrypt, argon2-cffi, or passlib. Example: from argon2 import PasswordHasher; ph = PasswordHasher(); hash = ph.hash(password).",
        "confidence": 0.88,
        "tags": ["OWASP:A02", "CWE-916"],
    },
    {
        "id": "python-insecure-deserialization-shelve",
        "pattern": r"shelve\.open\s*\([^)]*(?:request|param|query|user|input)",
        "severity": "high",
        "category": "injection",
        "language": "python",
        "description": "shelve.open() with user-controlled path in {file}. shelve uses pickle internally — path traversal + pickle deserialization is exploitable.",
        "suggestion": "Never use user-controlled paths with shelve. Use a database (SQLite, Redis) with parameterized access instead.",
        "confidence": 0.78,
        "tags": ["OWASP:A08", "CWE-502"],
    },
    {
        "id": "python-str-format-sql",
        "pattern": r"(?:execute|executemany)\s*\(\s*['\"][^'\"]*(?:%s|%d|%f)[^'\"]*['\"]\s*%\s*(?!\()",
        "severity": "critical",
        "category": "injection",
        "language": "python",
        "description": "String formatting used in SQL execute() in {file}. This is a SQL injection vulnerability even with %s placeholders if used incorrectly.",
        "suggestion": "Use proper parameterized queries: cursor.execute('SELECT * FROM t WHERE id = %s', (user_id,)). Note the tuple — never use Python string % operator.",
        "confidence": 0.82,
        "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "python-open-redirect-flask",
        "pattern": r"redirect\s*\([^)]*(?:request\.args|request\.form|request\.values|request\.get)",
        "severity": "high",
        "category": "auth",
        "language": "python",
        "description": "Open redirect in Flask — redirect() destination controlled by user input in {file}. Enables phishing attacks.",
        "suggestion": "Validate redirect URLs against an allowlist or use url_for(). Use urllib.parse.urlparse to ensure the destination host matches your domain.",
        "confidence": 0.80,
        "tags": ["OWASP:A01", "CWE-601"],
    },
    {
        "id": "python-jinja2-autoescape-off",
        "pattern": r"Environment\s*\([^)]*autoescape\s*=\s*False",
        "severity": "high",
        "category": "xss",
        "language": "python",
        "description": "Jinja2 Environment with autoescape=False in {file}. All template variables will be rendered without HTML escaping, enabling XSS.",
        "suggestion": "Set autoescape=True or use select_autoescape(['html', 'xml']). If you need raw HTML output, use Markup() explicitly on trusted content only.",
        "confidence": 0.92,
        "tags": ["OWASP:A03", "CWE-79"],
    },
    {
        "id": "python-xml-etree-parse-untrusted",
        "pattern": r"ET\.(?:parse|fromstring|XML)\s*\([^)]*(?:request|input|param|user|body)",
        "severity": "high",
        "category": "injection",
        "language": "python",
        "description": "xml.etree.ElementTree parsing potentially untrusted XML in {file}. Vulnerable to billion laughs (DoS) and other XML attacks.",
        "suggestion": "Use defusedxml library instead: import defusedxml.ElementTree as ET. It blocks XXE, entity expansion DoS, and other XML attacks.",
        "confidence": 0.78,
        "tags": ["OWASP:A03", "CWE-611"],
    },

    # ── Go-specific ───────────────────────────────────────────────────────────
    {
        "id": "go-sql-sprintf-injection",
        "pattern": r"fmt\.Sprintf\s*\([^)]*(?:SELECT|INSERT|UPDATE|DELETE|WHERE|FROM)[^)]*\)",
        "severity": "critical",
        "category": "injection",
        "language": "go",
        "description": "fmt.Sprintf used to build SQL query in {file}. String interpolation in SQL is a classic injection vector.",
        "suggestion": "Use parameterized queries: db.Query(\"SELECT * FROM users WHERE id = $1\", userID). Use sqlx or GORM for safer query building.",
        "confidence": 0.85,
        "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "go-exec-user-input",
        "pattern": r"exec\.Command\s*\([^)]*(?:r\.Form|r\.URL|r\.Query|Getenv|os\.Args)",
        "severity": "critical",
        "category": "injection",
        "language": "go",
        "description": "os/exec.Command called with potentially user-controlled arguments in {file}. Enables command injection.",
        "suggestion": "Sanitize and validate all arguments against a strict allowlist. Avoid passing user input to exec.Command. Use dedicated libraries for the task instead.",
        "confidence": 0.82,
        "tags": ["OWASP:A03", "CWE-78"],
    },
    {
        "id": "go-http-listen-all-interfaces",
        "pattern": r"http\.ListenAndServe\s*\(\s*['\"]:\d+['\"]",
        "severity": "medium",
        "category": "config",
        "language": "go",
        "description": "http.ListenAndServe binding to all interfaces (0.0.0.0) in {file}. Service is reachable from all network interfaces including public ones.",
        "suggestion": "Bind to specific interface if service is internal: http.ListenAndServe(\"127.0.0.1:8080\", handler). Use a reverse proxy (nginx) for public-facing services.",
        "confidence": 0.78,
        "tags": ["OWASP:A05", "CWE-605"],
    },
    {
        "id": "go-http-client-no-timeout",
        "pattern": r"&http\.Client\s*\{(?:[^}]*\n)*[^}]*\}(?![^{]*Timeout)",
        "severity": "medium",
        "category": "config",
        "language": "go",
        "description": "http.Client without Timeout in {file}. Connections can hang indefinitely, causing goroutine leaks and DoS.",
        "suggestion": "Always set a timeout: &http.Client{Timeout: 30 * time.Second}. Also set per-request deadlines via context: ctx, cancel := context.WithTimeout(ctx, 10*time.Second).",
        "confidence": 0.70,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "go-ioutil-readall-no-limit",
        "pattern": r"ioutil\.ReadAll\s*\(|io\.ReadAll\s*\(",
        "severity": "medium",
        "category": "config",
        "language": "go",
        "description": "ioutil.ReadAll()/io.ReadAll() without size limit in {file}. An attacker can send a huge body causing OOM.",
        "suggestion": "Wrap the reader: io.ReadAll(io.LimitReader(r.Body, 1<<20)). Set http.MaxBytesReader on request bodies: r.Body = http.MaxBytesReader(w, r.Body, 1048576).",
        "confidence": 0.72,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "go-template-html-bypass",
        "pattern": r"template\.HTML\s*\([^)]*(?:r\.Form|r\.URL|r\.Query|Getenv|userInput|input)",
        "severity": "high",
        "category": "xss",
        "language": "go",
        "description": "template.HTML() used with user-controlled data in {file}, bypassing Go's auto-escaping. Enables XSS.",
        "suggestion": "Never cast user-controlled strings to template.HTML. Let the html/template package handle escaping automatically.",
        "confidence": 0.85,
        "tags": ["OWASP:A03", "CWE-79"],
    },
    {
        "id": "go-unsafe-pointer-user-data",
        "pattern": r"unsafe\.Pointer\s*\([^)]*(?:input|user|request|param|query)",
        "severity": "critical",
        "category": "injection",
        "language": "go",
        "description": "unsafe.Pointer cast with potentially user-controlled data in {file}. Can cause memory corruption or arbitrary read/write.",
        "suggestion": "Never use unsafe.Pointer with user-controlled data. Refactor to use safe Go types. If pointer arithmetic is necessary, validate all bounds rigorously.",
        "confidence": 0.80,
        "tags": ["OWASP:A03", "CWE-119"],
    },
    {
        "id": "go-crypto-md5-sha1-password",
        "pattern": r"\"crypto/(?:md5|sha1)\"",
        "severity": "high",
        "category": "crypto",
        "language": "go",
        "description": "crypto/md5 or crypto/sha1 imported in {file}. These are not suitable for password hashing.",
        "suggestion": "For passwords, use golang.org/x/crypto/bcrypt or golang.org/x/crypto/argon2. For data integrity use crypto/sha256 or sha512.",
        "confidence": 0.75,
        "tags": ["OWASP:A02", "CWE-916"],
    },
    {
        "id": "go-panic-in-http-handler",
        "pattern": r"func\s+\w+\s*\(\s*\w+\s+http\.ResponseWriter\s*,\s*\w+\s+\*http\.Request\s*\)(?:[^}]*\n)*[^}]*\bpanic\s*\(",
        "severity": "medium",
        "category": "config",
        "language": "go",
        "description": "panic() in HTTP handler in {file} without recover(). An unhandled panic will crash the entire server process.",
        "suggestion": "Add a recovery middleware: defer func() { if r := recover(); r != nil { http.Error(w, \"Internal Error\", 500) } }(). Use frameworks like gin that include recovery middleware.",
        "confidence": 0.70,
        "tags": ["OWASP:A04", "CWE-248"],
    },
    {
        "id": "go-goroutine-leak",
        "pattern": r"go\s+func\s*\([^)]*\)\s*\{(?:[^}]*\n)*[^}]*\}(?!\s*\(\s*\))(?![^{]*(?:done|quit|stop|cancel|ctx\.Done|stopCh))",
        "severity": "medium",
        "category": "config",
        "language": "go",
        "description": "Goroutine launched without cancellation signal in {file}. This can cause goroutine leaks, gradually exhausting system resources.",
        "suggestion": "Pass a context.Context to goroutines and select on ctx.Done(). Use done channels to signal goroutines to stop. Always ensure goroutines have a termination path.",
        "confidence": 0.55,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "go-missing-context-cancellation",
        "pattern": r"context\.Background\s*\(\s*\)(?!\s*//.*cancel)",
        "severity": "low",
        "category": "config",
        "language": "go",
        "description": "context.Background() used without timeout or cancellation in {file}. Long-running operations should use cancelable contexts.",
        "suggestion": "Use context.WithTimeout or context.WithCancel: ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second); defer cancel().",
        "confidence": 0.50,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "go-integer-overflow-no-check",
        "pattern": r"int\s*\(\s*(?:r\.Form|r\.URL\.Query|strconv\.Atoi|req\.Form)\s*\[['\"][^'\"]+['\"]\]\s*\[",
        "severity": "medium",
        "category": "injection",
        "language": "go",
        "description": "Integer conversion from user input without bounds checking in {file}. Can lead to integer overflow or array out-of-bounds access.",
        "suggestion": "Always validate parsed integers: if val < 0 || val > maxAllowed { return error }. Use strconv.ParseInt with bitSize parameter to enforce bounds.",
        "confidence": 0.68,
        "tags": ["OWASP:A03", "CWE-190"],
    },
    {
        "id": "go-sql-string-concat",
        "pattern": r"db\.(?:Query|Exec|QueryRow)\s*\(\s*[\"'`][^\"'`]*\s*\+",
        "severity": "critical",
        "category": "injection",
        "language": "go",
        "description": "SQL query built with string concatenation in {file}. Direct SQL injection vulnerability.",
        "suggestion": "Use $1, $2 placeholders: db.Query(\"SELECT * FROM t WHERE id=$1\", id). Or use a query builder like squirrel or GORM with parameterization.",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "go-tls-insecure-skip-verify",
        "pattern": r"InsecureSkipVerify\s*:\s*true",
        "severity": "high",
        "category": "crypto",
        "language": "go",
        "description": "TLS InsecureSkipVerify=true in {file}. Disables all TLS certificate validation, enabling MITM attacks.",
        "suggestion": "Remove InsecureSkipVerify. If using custom CA: tls.Config{RootCAs: certPool}. For testing use a test CA, never disable verification.",
        "confidence": 0.95,
        "tags": ["OWASP:A02", "CWE-295"],
    },
    {
        "id": "go-race-condition-global-write",
        "pattern": r"(?:var\s+\w+\s*=|map\[string\]\w+\{)(?:[^}]*\n){0,5}go\s+func",
        "severity": "medium",
        "category": "config",
        "language": "go",
        "description": "Potential data race: shared variable written in goroutine in {file}. Run with -race flag to confirm.",
        "suggestion": "Protect shared state with sync.Mutex, sync.RWMutex, or use sync.Map for concurrent map access. Consider channels for communication between goroutines.",
        "confidence": 0.50,
        "tags": ["OWASP:A04", "CWE-362"],
    },
    {
        "id": "go-http-redirect-open",
        "pattern": r"http\.Redirect\s*\([^,]+,\s*[^,]+,\s*(?:r\.URL\.Query|r\.FormValue|r\.PostFormValue)",
        "severity": "high",
        "category": "auth",
        "language": "go",
        "description": "Open redirect in {file}: redirect destination controlled by user input. Enables phishing attacks.",
        "suggestion": "Validate redirect URL against an allowlist of internal paths. Use url.Parse and check the host is empty or matches your domain.",
        "confidence": 0.82,
        "tags": ["OWASP:A01", "CWE-601"],
    },

    # ── Java / Spring-specific ────────────────────────────────────────────────
    {
        "id": "java-log4shell-jndi",
        "pattern": r"\$\{(?:jndi|j(?:ndi|ndi):(?:ldap|rmi|dns|iiop|corba|nds|http))",
        "severity": "critical",
        "category": "injection",
        "language": "java",
        "description": "Log4Shell JNDI injection pattern detected in {file}. This is the CVE-2021-44228 attack payload.",
        "suggestion": "Update log4j to >= 2.17.1 immediately. Set log4j2.formatMsgNoLookups=true as mitigation. Remove/block ${jndi: patterns at WAF.",
        "confidence": 0.97,
        "tags": ["OWASP:A03", "CWE-917"],
        "cve_id": "CVE-2021-44228",
    },
    {
        "id": "java-spel-injection",
        "pattern": r"(?:ExpressionParser|SpelExpressionParser|EvaluationContext).*(?:parseExpression|getValue)\s*\([^)]*(?:request|param|input|user)",
        "severity": "critical",
        "category": "injection",
        "language": "java",
        "description": "Spring SpEL injection in {file}. User-controlled input in SpEL expressions allows arbitrary code execution.",
        "suggestion": "Never evaluate user input as SpEL. Use SimpleEvaluationContext instead of StandardEvaluationContext when user data may be involved. Validate and sanitize all inputs.",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "java-object-deserialization",
        "pattern": r"ObjectInputStream\s*\(\s*(?:request|socket|input|stream|getInputStream)",
        "severity": "critical",
        "category": "injection",
        "language": "java",
        "description": "Java deserialization via ObjectInputStream from network/request source in {file}. Enables Remote Code Execution.",
        "suggestion": "Avoid Java serialization. Use JSON/protobuf. If serialization is required, implement a resolveClass whitelist. Use SerialKiller or NotSoSerial agent.",
        "confidence": 0.90,
        "tags": ["OWASP:A08", "CWE-502"],
    },
    {
        "id": "java-jndi-user-input",
        "pattern": r"InitialContext\s*\(\s*\).*lookup\s*\([^)]*(?:request|param|input|user)",
        "severity": "critical",
        "category": "injection",
        "language": "java",
        "description": "JNDI lookup with user-controlled name in {file}. Allows SSRF and potentially RCE via remote class loading.",
        "suggestion": "Never use user input in JNDI lookups. Disable remote class loading: com.sun.jndi.ldap.object.trustURLCodebase=false (default in Java 8u191+).",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-74"],
    },
    {
        "id": "java-runtime-exec-user-input",
        "pattern": r"Runtime\.getRuntime\s*\(\s*\)\.exec\s*\([^)]*(?:request|param|input|user|getParameter)",
        "severity": "critical",
        "category": "injection",
        "language": "java",
        "description": "Runtime.exec() with potentially user-controlled input in {file}. Direct OS command injection.",
        "suggestion": "Never pass user input to Runtime.exec(). Use ProcessBuilder with an argument array: new ProcessBuilder(\"cmd\", safeArg). Validate all arguments strictly.",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-78"],
    },
    {
        "id": "java-reflection-user-class",
        "pattern": r"Class\.forName\s*\([^)]*(?:request|param|input|user|getParameter)",
        "severity": "critical",
        "category": "injection",
        "language": "java",
        "description": "Class.forName() with user-controlled class name in {file}. Enables loading arbitrary classes and code execution.",
        "suggestion": "Use a strict allowlist of permitted class names. Never let user input dictate which class to load via reflection.",
        "confidence": 0.85,
        "tags": ["OWASP:A03", "CWE-470"],
    },
    {
        "id": "java-xxe-documentbuilder",
        "pattern": r"DocumentBuilderFactory\.newInstance\s*\(\s*\)(?:[^;]*\n){0,5}(?![^;]*setFeature[^;]*XMLConstants)",
        "severity": "high",
        "category": "injection",
        "language": "java",
        "description": "DocumentBuilderFactory without XXE protection in {file}. Parses XML with external entity resolution enabled by default.",
        "suggestion": "Disable XXE: factory.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, true); factory.setFeature(\"http://xml.org/sax/features/external-general-entities\", false);",
        "confidence": 0.78,
        "tags": ["OWASP:A03", "CWE-611"],
    },
    {
        "id": "java-ssrf-url-openconnection",
        "pattern": r"new\s+URL\s*\([^)]*(?:request|param|input|user|getParameter)\s*\).*openConnection",
        "severity": "high",
        "category": "ssrf",
        "language": "java",
        "description": "SSRF vulnerability in {file}: URL opened from user-controlled input. Allows scanning internal network.",
        "suggestion": "Validate URL against an allowlist of permitted domains. Check the resolved IP is not in private ranges (10.x, 192.168.x, 172.16-31.x, 127.x). Use a dedicated SSRF-safe HTTP client.",
        "confidence": 0.85,
        "tags": ["OWASP:A10", "CWE-918"],
    },
    {
        "id": "java-spring-missing-preauthorize",
        "pattern": r"@(?:GetMapping|PostMapping|PutMapping|DeleteMapping|RequestMapping)\s*\([^)]*\)(?:\s*\n\s*public)(?!\s*@PreAuthorize)",
        "severity": "medium",
        "category": "auth",
        "language": "java",
        "description": "Spring endpoint without @PreAuthorize authorization in {file}. Endpoint may be accessible without proper authorization.",
        "suggestion": "Add @PreAuthorize(\"hasRole('USER')\") or @Secured annotation. Use method security globally via @EnableMethodSecurity.",
        "confidence": 0.60,
        "tags": ["OWASP:A01", "CWE-862"],
    },
    {
        "id": "java-h2-console-enabled",
        "pattern": r"(?i)spring\.h2\.console\.enabled\s*=\s*true",
        "severity": "high",
        "category": "config",
        "language": "java",
        "description": "H2 database web console enabled in {file}. Accessible H2 console allows executing arbitrary SQL and file system access.",
        "suggestion": "Disable in production: spring.h2.console.enabled=false. If needed in dev, restrict access: spring.h2.console.settings.web-allow-others=false.",
        "confidence": 0.95,
        "tags": ["OWASP:A05", "CWE-16"],
    },
    {
        "id": "java-actuator-exposed",
        "pattern": r"management\.endpoints\.web\.exposure\.include\s*=\s*\*",
        "severity": "high",
        "category": "config",
        "language": "java",
        "description": "Spring Actuator exposes all endpoints in {file}. /actuator/env, /actuator/heapdump, /actuator/shutdown can leak secrets or crash the app.",
        "suggestion": "Expose only necessary endpoints: management.endpoints.web.exposure.include=health,info. Secure endpoints with Spring Security. Move to non-public port.",
        "confidence": 0.95,
        "tags": ["OWASP:A05", "CWE-200"],
    },
    {
        "id": "java-spring-csrf-disabled",
        "pattern": r"\.csrf\s*\(\s*\)\s*\.disable\s*\(\s*\)",
        "severity": "high",
        "category": "auth",
        "language": "java",
        "description": "Spring Security CSRF protection disabled in {file}. State-changing requests are vulnerable to cross-site request forgery.",
        "suggestion": "Remove .csrf().disable(). Use Spring's default CSRF protection with synchronizer tokens. For REST APIs with JWT, use SameSite=Strict cookies instead.",
        "confidence": 0.92,
        "tags": ["OWASP:A01", "CWE-352"],
    },
    {
        "id": "java-string-format-sql",
        "pattern": r"String\.format\s*\(\s*[\"'][^\"']*(?:SELECT|INSERT|UPDATE|DELETE|WHERE)[^\"']*[\"']",
        "severity": "critical",
        "category": "injection",
        "language": "java",
        "description": "String.format() used to build SQL query in {file}. Classic SQL injection vulnerability.",
        "suggestion": "Use PreparedStatement: PreparedStatement ps = conn.prepareStatement(\"SELECT * FROM t WHERE id=?\"); ps.setInt(1, id);",
        "confidence": 0.85,
        "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "java-spring-requestmapping-no-method",
        "pattern": r"@RequestMapping\s*\(\s*[\"']/",
        "severity": "low",
        "category": "config",
        "language": "java",
        "description": "@RequestMapping without method restriction in {file}. Endpoint accepts all HTTP methods including DELETE, PUT, PATCH.",
        "suggestion": "Specify the HTTP method: @RequestMapping(value=\"/path\", method=RequestMethod.GET) or use @GetMapping/@PostMapping.",
        "confidence": 0.70,
        "tags": ["OWASP:A05", "CWE-16"],
    },
    {
        "id": "java-hardcoded-credentials-properties",
        "pattern": r"(?i)(?:spring\.datasource\.password|spring\.security\.user\.password)\s*=\s*(?!\\$\{)[^\s]{4,}",
        "severity": "critical",
        "category": "secrets",
        "language": "java",
        "description": "Hardcoded database/user password in Spring application.properties in {file}.",
        "suggestion": "Use environment variable references: spring.datasource.password=${DB_PASSWORD}. Store secrets in Vault, AWS Secrets Manager, or Kubernetes secrets.",
        "confidence": 0.88,
        "tags": ["OWASP:A02", "CWE-798"],
    },
    {
        "id": "java-ognl-injection-struts",
        "pattern": r"(?:OgnlUtil|Ognl\.getValue|ActionContext\.getContext).*(?:request|param|input|user)",
        "severity": "critical",
        "category": "injection",
        "language": "java",
        "description": "Potential OGNL injection in Struts in {file}. OGNL with user input enables RCE (CVE-2017-5638 class).",
        "suggestion": "Update Apache Struts to latest version. Validate all input before OGNL evaluation. Use strict OGNL ExcludedClasses/ExcludedPackages configuration.",
        "confidence": 0.82,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "java-insecure-cookie",
        "pattern": r"new\s+Cookie\s*\([^)]+\)(?:[^;]*\n){0,3}(?![^;]*(?:setHttpOnly|setSecure)\s*\(\s*true)",
        "severity": "medium",
        "category": "auth",
        "language": "java",
        "description": "Cookie created without HttpOnly/Secure flags in {file}. Accessible to JavaScript and transmittable over plain HTTP.",
        "suggestion": "Set both flags: cookie.setHttpOnly(true); cookie.setSecure(true); Also set SameSite via response header: response.setHeader(\"Set-Cookie\", \"...;SameSite=Strict\");",
        "confidence": 0.72,
        "tags": ["OWASP:A02", "CWE-614"],
    },
    {
        "id": "java-jsp-scriptlets",
        "pattern": r"<%[^=!@][^%]*(?:request\.getParameter|request\.getAttribute|out\.print)[^%]*%>",
        "severity": "medium",
        "category": "xss",
        "language": "java",
        "description": "JSP scriptlet with potential unescaped output in {file}. Legacy scriptlets are XSS-prone and hard to audit.",
        "suggestion": "Use JSTL with c:out for escaped output: <c:out value=\"${param.name}\"/>. Avoid scriptlets in favor of EL expressions and JSTL.",
        "confidence": 0.70,
        "tags": ["OWASP:A03", "CWE-79"],
    },

    # ── PHP-specific ──────────────────────────────────────────────────────────
    {
        "id": "php-eval-user-input",
        "pattern": r"eval\s*\(\s*(?:\$_GET|\$_POST|\$_REQUEST|\$_COOKIE|\$_SERVER)",
        "severity": "critical",
        "category": "injection",
        "language": "php",
        "description": "eval() with direct user input in {file}. This is arbitrary PHP code execution.",
        "suggestion": "Never eval user input. If dynamic code execution seems necessary, redesign using data-driven config. eval() has virtually no safe use case with user data.",
        "confidence": 0.97,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "php-system-exec-user-input",
        "pattern": r"(?:system|exec|passthru|shell_exec|popen|proc_open)\s*\(\s*(?:\$_GET|\$_POST|\$_REQUEST|.*\$_GET|.*\$_POST)",
        "severity": "critical",
        "category": "injection",
        "language": "php",
        "description": "OS command function called with user input in {file}. Direct command injection vulnerability.",
        "suggestion": "Avoid shell functions with user data. If necessary, use escapeshellarg(): system('ls ' . escapeshellarg($dir)). Better: use PHP native functions for the task.",
        "confidence": 0.92,
        "tags": ["OWASP:A03", "CWE-78"],
    },
    {
        "id": "php-include-user-input",
        "pattern": r"(?:include|require|include_once|require_once)\s*(?:\(?\s*)(?:\$_GET|\$_POST|\$_REQUEST|\$_COOKIE|\$[a-zA-Z]+)",
        "severity": "critical",
        "category": "injection",
        "language": "php",
        "description": "Dynamic file inclusion with user-controlled path in {file}. Enables Local File Inclusion (LFI) and Remote File Inclusion (RFI).",
        "suggestion": "Use a static whitelist: $pages = ['home','about','contact']; if (!in_array($page, $pages)) die('Invalid page'). Never include based on user input directly.",
        "confidence": 0.90,
        "tags": ["OWASP:A03", "CWE-98"],
    },
    {
        "id": "php-sql-concat-user-input",
        "pattern": r"(?:mysql_query|mysqli_query|->query)\s*\([^)]*(?:\$_GET|\$_POST|\$_REQUEST|\$_COOKIE)",
        "severity": "critical",
        "category": "injection",
        "language": "php",
        "description": "SQL query with direct user input concatenation in {file}. Classic SQL injection.",
        "suggestion": "Use PDO with prepared statements: $stmt = $pdo->prepare('SELECT * FROM t WHERE id = ?'); $stmt->execute([$_GET['id']]);",
        "confidence": 0.92,
        "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "php-server-php-self-xss",
        "pattern": r"\$_SERVER\s*\[\s*['\"]PHP_SELF['\"]\s*\]",
        "severity": "high",
        "category": "xss",
        "language": "php",
        "description": "$_SERVER['PHP_SELF'] used unescaped in {file}. Attackers can inject JavaScript via crafted URLs.",
        "suggestion": "Always escape: htmlspecialchars($_SERVER['PHP_SELF'], ENT_QUOTES, 'UTF-8'). Or use a router that generates safe URLs.",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-79"],
    },
    {
        "id": "php-extract-user-input",
        "pattern": r"extract\s*\(\s*(?:\$_GET|\$_POST|\$_REQUEST|\$_COOKIE)",
        "severity": "critical",
        "category": "injection",
        "language": "php",
        "description": "extract() with user input in {file}. Allows overwriting any variable in scope, including auth flags.",
        "suggestion": "Never call extract() on user-supplied arrays. Access individual keys explicitly: $name = $_POST['name'] ?? '';",
        "confidence": 0.97,
        "tags": ["OWASP:A03", "CWE-95"],
    },
    {
        "id": "php-missing-htmlspecialchars",
        "pattern": r"echo\s+(?:\$_GET|\$_POST|\$_REQUEST|\$_COOKIE|\$_SERVER)",
        "severity": "high",
        "category": "xss",
        "language": "php",
        "description": "Direct echo of user input without escaping in {file}. Cross-site scripting vulnerability.",
        "suggestion": "Always escape output: echo htmlspecialchars($var, ENT_QUOTES | ENT_HTML5, 'UTF-8'). Use a template engine with auto-escaping (Twig, Blade).",
        "confidence": 0.90,
        "tags": ["OWASP:A03", "CWE-79"],
    },
    {
        "id": "php-md5-sha1-password",
        "pattern": r"(?:md5|sha1)\s*\(\s*(?:\$password|\$pass|\$pwd|\$_POST\s*\[)",
        "severity": "high",
        "category": "crypto",
        "language": "php",
        "description": "md5()/sha1() used for password hashing in {file}. These are broken for password storage.",
        "suggestion": "Use password_hash(): $hash = password_hash($password, PASSWORD_ARGON2ID). Verify with password_verify($input, $hash). Migrate existing MD5/SHA1 hashes.",
        "confidence": 0.90,
        "tags": ["OWASP:A02", "CWE-916"],
    },
    {
        "id": "php-mysql-deprecated",
        "pattern": r"mysql_(?:query|connect|fetch|num_rows|escape_string)\s*\(",
        "severity": "high",
        "category": "injection",
        "language": "php",
        "description": "Deprecated mysql_*() functions used in {file}. Removed in PHP 7 and lacks prepared statement support.",
        "suggestion": "Migrate to PDO or MySQLi with prepared statements. PDO: $stmt = $pdo->prepare('SELECT * FROM t WHERE id=?'); $stmt->execute([$id]);",
        "confidence": 0.95,
        "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "php-preg-replace-e-modifier",
        "pattern": r"preg_replace\s*\(\s*['\"][^'\"]*\/e['\"]",
        "severity": "critical",
        "category": "injection",
        "language": "php",
        "description": "preg_replace() with /e (eval) modifier in {file}. The replacement string is evaluated as PHP code — RCE if user-controlled.",
        "suggestion": "Replace with preg_replace_callback(): preg_replace_callback($pattern, function($m) { return safe_transform($m[1]); }, $subject). The /e modifier was removed in PHP 7.",
        "confidence": 0.95,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "php-unserialize-user-input",
        "pattern": r"unserialize\s*\(\s*(?:\$_GET|\$_POST|\$_REQUEST|\$_COOKIE|.*\$_GET|.*\$_POST)",
        "severity": "critical",
        "category": "injection",
        "language": "php",
        "description": "unserialize() with user-controlled data in {file}. PHP object injection can lead to RCE via magic methods.",
        "suggestion": "Never unserialize untrusted data. Use JSON: json_decode($input, true). If serialization is needed, use HMAC signing: only unserialize if HMAC validates.",
        "confidence": 0.95,
        "tags": ["OWASP:A08", "CWE-502"],
    },
    {
        "id": "php-file-upload-no-mime-check",
        "pattern": r"move_uploaded_file\s*\([^,]+,\s*[^)]+\)(?![^;]*(?:mime|type|extension|getimagesize|finfo))",
        "severity": "high",
        "category": "injection",
        "language": "php",
        "description": "move_uploaded_file() without MIME type validation in {file}. Allows uploading PHP webshells.",
        "suggestion": "Validate with finfo: $finfo = new finfo(FILEINFO_MIME_TYPE); $type = $finfo->file($_FILES['f']['tmp_name']). Check extension whitelist. Store outside webroot.",
        "confidence": 0.78,
        "tags": ["OWASP:A03", "CWE-434"],
    },
    {
        "id": "php-phpinfo-production",
        "pattern": r"phpinfo\s*\(\s*\)",
        "severity": "high",
        "category": "config",
        "language": "php",
        "description": "phpinfo() call in {file}. Exposes PHP version, loaded modules, server paths, and environment variables to attackers.",
        "suggestion": "Remove phpinfo() from production code. Use a separate authenticated admin page if debugging info is needed. Add to code review checklist.",
        "confidence": 0.95,
        "tags": ["OWASP:A05", "CWE-200"],
    },
    {
        "id": "php-missing-session-regenerate",
        "pattern": r"session_start\s*\(\s*\)(?![^;{]*session_regenerate_id)",
        "severity": "medium",
        "category": "auth",
        "language": "php",
        "description": "session_start() after login without session_regenerate_id() in {file}. Session fixation vulnerability.",
        "suggestion": "After successful login, call: session_regenerate_id(true). This generates a new session ID and deletes the old one, preventing session fixation.",
        "confidence": 0.65,
        "tags": ["OWASP:A07", "CWE-384"],
    },
    {
        "id": "php-open-redirect",
        "pattern": r"header\s*\(\s*['\"]Location:\s*['\"]?\s*\.\s*(?:\$_GET|\$_POST|\$_REQUEST)",
        "severity": "high",
        "category": "auth",
        "language": "php",
        "description": "Open redirect via header() with user-controlled URL in {file}. Enables phishing attacks.",
        "suggestion": "Validate redirect URL against an allowlist of permitted paths. Never redirect to user-supplied URLs without validation.",
        "confidence": 0.88,
        "tags": ["OWASP:A01", "CWE-601"],
    },

    # ── Ruby on Rails-specific ────────────────────────────────────────────────
    {
        "id": "rails-render-inline-user-input",
        "pattern": r"render\s+inline:\s*(?:params|request|session|cookies)",
        "severity": "critical",
        "category": "injection",
        "language": "ruby",
        "description": "render inline: with user input in {file}. Allows Server-Side Template Injection (SSTI) and arbitrary code execution.",
        "suggestion": "Never use render inline: with user data. Use render template: or render partial: with safe template files. Sanitize any dynamic content.",
        "confidence": 0.92,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "rails-eval-params",
        "pattern": r"\beval\s+(?:params|request|session|cookies)",
        "severity": "critical",
        "category": "injection",
        "language": "ruby",
        "description": "eval() with user-supplied params in {file}. Arbitrary Ruby code execution.",
        "suggestion": "Never eval user input. Redesign to use case/when with a known set of actions, or a safe expression evaluator.",
        "confidence": 0.95,
        "tags": ["OWASP:A03", "CWE-94"],
    },
    {
        "id": "rails-send-user-input",
        "pattern": r"\.send\s*\(\s*params\s*\[",
        "severity": "critical",
        "category": "injection",
        "language": "ruby",
        "description": "Object.send() with user-controlled method name in {file}. Allows calling any method on the object, including private ones.",
        "suggestion": "Use an explicit whitelist: allowed = ['method_a', 'method_b']; obj.send(allowed.find { |m| m == params[:action] }) || raise. Never send arbitrary param values as method names.",
        "confidence": 0.90,
        "tags": ["OWASP:A03", "CWE-470"],
    },
    {
        "id": "rails-sql-interpolation",
        "pattern": r"\.where\s*\(\s*[\"'].*#\{(?:params|request|session|cookies)",
        "severity": "critical",
        "category": "injection",
        "language": "ruby",
        "description": "ActiveRecord where() with string interpolation of user data in {file}. Direct SQL injection.",
        "suggestion": "Use parameterized form: where('name = ?', params[:name]) or hash syntax: where(name: params[:name]). Never interpolate user input into SQL strings.",
        "confidence": 0.92,
        "tags": ["OWASP:A03", "CWE-89"],
    },
    {
        "id": "rails-open-redirect",
        "pattern": r"redirect_to\s+params\s*\[",
        "severity": "high",
        "category": "auth",
        "language": "ruby",
        "description": "Open redirect via redirect_to with user-controlled URL in {file}. Enables phishing attacks.",
        "suggestion": "Validate the redirect target: redirect_to only_path: true or validate against known safe paths. Use url_for with explicit controller/action instead.",
        "confidence": 0.90,
        "tags": ["OWASP:A01", "CWE-601"],
    },
    {
        "id": "rails-missing-protect-from-forgery",
        "pattern": r"class\s+\w+Controller[^<]*<[^<]*ApplicationController(?:[^e]|e[^n]|en[^d])*?end(?![\s\S]*protect_from_forgery)",
        "severity": "high",
        "category": "auth",
        "language": "ruby",
        "description": "Controller in {file} may be missing protect_from_forgery. CSRF protection may not be active.",
        "suggestion": "Ensure ApplicationController includes protect_from_forgery with: exception. Verify it is not being skipped for sensitive actions.",
        "confidence": 0.55,
        "tags": ["OWASP:A01", "CWE-352"],
    },
    {
        "id": "rails-system-exec-user-input",
        "pattern": r"(?:system|exec|`|\%x)\s*(?:\(?\s*)(?:params|request\.params|params\[)",
        "severity": "critical",
        "category": "injection",
        "language": "ruby",
        "description": "Shell execution with user-controlled input in {file}. Command injection vulnerability.",
        "suggestion": "Use Ruby native libraries instead of shell commands. If shell is necessary, use Open3.capture2 with explicit arg array to avoid shell interpolation.",
        "confidence": 0.88,
        "tags": ["OWASP:A03", "CWE-78"],
    },
    {
        "id": "rails-yaml-load-user-input",
        "pattern": r"YAML\.load\s*\([^)]*(?:params|request|body|input)",
        "severity": "critical",
        "category": "injection",
        "language": "ruby",
        "description": "YAML.load() with user-controlled input in {file}. Allows deserialization of arbitrary Ruby objects — RCE.",
        "suggestion": "Use YAML.safe_load() which only loads basic types. Never use YAML.load() with untrusted input.",
        "confidence": 0.92,
        "tags": ["OWASP:A08", "CWE-502"],
        "cve_id": "CVE-2013-0156",
    },
    {
        "id": "rails-hardcoded-secret-key-base",
        "pattern": r"secret_key_base\s*[:=]\s*['\"][a-f0-9]{20,}['\"]",
        "severity": "critical",
        "category": "secrets",
        "language": "ruby",
        "description": "Hardcoded secret_key_base in Rails config in {file}. All sessions and signed cookies can be forged.",
        "suggestion": "Use Rails credentials or environment variable: secret_key_base: <%= ENV['SECRET_KEY_BASE'] %>. Run rails secret to generate a new key.",
        "confidence": 0.90,
        "tags": ["OWASP:A02", "CWE-321"],
    },
    {
        "id": "rails-mass-assignment",
        "pattern": r"def\s+\w+_params(?:[^e]|e[^n]|en[^d])*?end(?![\s\S]{0,200}permit\s*\()",
        "severity": "medium",
        "category": "auth",
        "language": "ruby",
        "description": "Strong parameters method in {file} may not use permit(). Mass assignment may allow setting protected attributes.",
        "suggestion": "Always use permit() to whitelist: def user_params; params.require(:user).permit(:name, :email); end. Never use params.permit! which allows everything.",
        "confidence": 0.55,
        "tags": ["OWASP:A04", "CWE-915"],
    },

    # ── Rust-specific ─────────────────────────────────────────────────────────
    {
        "id": "rust-unsafe-user-data",
        "pattern": r"unsafe\s*\{[^}]*(?:user_input|request|param|query|body|stdin)",
        "severity": "critical",
        "category": "injection",
        "language": "rust",
        "description": "unsafe block operating on potentially user-controlled data in {file}. Memory safety guarantees are suspended.",
        "suggestion": "Validate and sanitize all user data before entering unsafe block. Minimize unsafe scope. Use safe abstractions (slices, Vec bounds checks) where possible.",
        "confidence": 0.78,
        "tags": ["OWASP:A03", "CWE-119"],
    },
    {
        "id": "rust-unwrap-production",
        "pattern": r"\.unwrap\s*\(\s*\)(?!\s*;?\s*//.*test)",
        "severity": "medium",
        "category": "config",
        "language": "rust",
        "description": "unwrap() in production code in {file}. Will panic on None/Err, causing process crash — potential DoS if on a request path.",
        "suggestion": "Use proper error handling: .unwrap_or_default(), .unwrap_or_else(|e| handle(e)), or propagate with ?. Reserve unwrap() for tests and prototypes only.",
        "confidence": 0.65,
        "tags": ["OWASP:A04", "CWE-248"],
    },
    {
        "id": "rust-command-user-input",
        "pattern": r"std::process::Command::new\s*\([^)]*(?:user_input|request|param|query|args|stdin)",
        "severity": "critical",
        "category": "injection",
        "language": "rust",
        "description": "std::process::Command with user-controlled arguments in {file}. Command injection risk.",
        "suggestion": "Validate arguments against a strict allowlist before passing to Command. Use .arg() method instead of .args() with user-controlled slices to avoid shell interpretation.",
        "confidence": 0.82,
        "tags": ["OWASP:A03", "CWE-78"],
    },
    {
        "id": "rust-integer-overflow-release",
        "pattern": r"(?:u8|u16|u32|u64|i8|i16|i32|i64)\s*::MAX\s*-\s*\w+|as\s+(?:u8|u16|u32|u64|i8|i16|i32|i64)\b",
        "severity": "medium",
        "category": "injection",
        "language": "rust",
        "description": "Potentially unchecked integer cast/arithmetic in {file}. Release builds disable overflow checks — silent truncation or wrap-around.",
        "suggestion": "Use checked arithmetic: value.checked_sub(n).ok_or(Error::Overflow)?. Or use saturating_sub/saturating_add. Enable overflow-checks = true in Cargo.toml for release.",
        "confidence": 0.60,
        "tags": ["OWASP:A03", "CWE-190"],
    },
    {
        "id": "rust-missing-error-handling",
        "pattern": r"let\s+\w+\s*=\s*\w+.*\.expect\s*\(\s*['\"][^'\"]+['\"]\s*\)(?!\s*;?\s*//.*test)",
        "severity": "low",
        "category": "config",
        "language": "rust",
        "description": "expect() used in {file}. Like unwrap(), it panics on failure — potential DoS in production request paths.",
        "suggestion": "Replace .expect(\"msg\") with proper error propagation using the ? operator or .unwrap_or_else(). Keep .expect() only in tests.",
        "confidence": 0.60,
        "tags": ["OWASP:A04", "CWE-248"],
    },

    # ── GraphQL-specific ──────────────────────────────────────────────────────
    {
        "id": "graphql-introspection-enabled",
        "pattern": r"(?i)introspection\s*[:=]\s*true",
        "severity": "medium",
        "category": "config",
        "description": "GraphQL introspection enabled in {file}. In production, introspection reveals the full schema to attackers.",
        "suggestion": "Disable introspection in production: introspection: process.env.NODE_ENV !== 'production'. Use persisted queries for production API.",
        "confidence": 0.80,
        "tags": ["OWASP:A05", "CWE-200"],
    },
    {
        "id": "graphql-no-depth-limit",
        "pattern": r"(?:ApolloServer|createSchema|buildSchema|makeExecutableSchema)\s*\(",
        "severity": "medium",
        "category": "config",
        "description": "GraphQL server in {file} may lack query depth limiting. Deeply nested queries can cause DoS.",
        "suggestion": "Add graphql-depth-limit: import depthLimit from 'graphql-depth-limit'; validationRules: [depthLimit(10)]. Also add query complexity limiting.",
        "confidence": 0.55,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "graphql-no-complexity-limit",
        "pattern": r"(?:ApolloServer|graphqlHTTP|createYoga)\s*\(\s*\{(?:[^}]*\n){0,10}[^}]*\}(?![^)]*complexity)",
        "severity": "medium",
        "category": "config",
        "description": "GraphQL server without query complexity limiting in {file}. Expensive queries can exhaust server resources.",
        "suggestion": "Use graphql-query-complexity: add createComplexityLimitRule(1000) to validationRules. Assign complexity scores to each field resolver.",
        "confidence": 0.50,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "graphql-batch-attacks",
        "pattern": r"(?i)batching\s*[:=]\s*true(?![^;]*limit)",
        "severity": "medium",
        "category": "config",
        "description": "GraphQL batching enabled without limits in {file}. Batch queries can bypass rate limiting and amplify attacks.",
        "suggestion": "Limit batch size: configure max batch size in apollo-server or use graphql-batch-limiter. Also implement per-query rate limiting.",
        "confidence": 0.70,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "graphql-mutation-no-auth",
        "pattern": r"(?:Mutation|mutation)\s*:\s*\{[^}]*resolve\s*:\s*(?:async\s*)?\([^)]*\)\s*=>\s*\{(?![^}]*(?:auth|user|context\.user|isAuthenticated))",
        "severity": "high",
        "category": "auth",
        "description": "GraphQL mutation resolver without authentication check in {file}. Mutations may be callable by unauthenticated users.",
        "suggestion": "Check authentication in every mutation resolver: if (!context.user) throw new AuthenticationError('Not authenticated'). Use a schema directive or middleware for consistency.",
        "confidence": 0.60,
        "tags": ["OWASP:A01", "CWE-862"],
    },
    {
        "id": "graphql-no-rate-limiting",
        "pattern": r"(?:ApolloServer|createYoga|graphqlHTTP)\s*\(\s*\{(?:[^}]*\n){0,15}[^}]*\}(?![\s\S]{0,200}(?:rateLimit|rateLimiter|throttle))",
        "severity": "medium",
        "category": "config",
        "description": "GraphQL endpoint in {file} without rate limiting. Susceptible to abuse and DoS.",
        "suggestion": "Add rate limiting: use graphql-rate-limit or implement at the resolver level. Also add IP-based throttling at the API gateway.",
        "confidence": 0.45,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "graphql-schema-exposed",
        "pattern": r"__(?:schema|type)\s*\{",
        "severity": "low",
        "category": "config",
        "description": "GraphQL introspection query __schema/__type in {file}. If this is client-facing code, it may reveal full schema.",
        "suggestion": "Disable introspection in production GraphQL servers. Monitor for introspection queries in production logs.",
        "confidence": 0.60,
        "tags": ["OWASP:A05", "CWE-200"],
    },

    # ── gRPC-specific ─────────────────────────────────────────────────────────
    {
        "id": "grpc-insecure-channel",
        "pattern": r"grpc\.insecure_channel\s*\(|grpc\.dial\s*\([^)]*grpc\.WithInsecure\s*\(\s*\)",
        "severity": "high",
        "category": "crypto",
        "description": "gRPC insecure channel (no TLS) in {file}. All RPC traffic is transmitted in plaintext.",
        "suggestion": "Use TLS: grpc.secure_channel(address, grpc.ssl_channel_credentials()) in Python, or grpc.Dial(addr, grpc.WithTransportCredentials(creds)) in Go.",
        "confidence": 0.90,
        "tags": ["OWASP:A02", "CWE-319"],
    },
    {
        "id": "grpc-no-auth-interceptor",
        "pattern": r"grpc\.server\s*\(\s*\)(?!\s*\n.*(?:interceptor|auth|authenticat))",
        "severity": "high",
        "category": "auth",
        "description": "gRPC server created without authentication interceptor in {file}. All RPCs may be callable without auth.",
        "suggestion": "Add a server-side interceptor: grpc.server(interceptors=[auth_interceptor]). Validate JWT/mTLS certificates in the interceptor before processing any RPC.",
        "confidence": 0.65,
        "tags": ["OWASP:A01", "CWE-306"],
    },
    {
        "id": "grpc-no-deadline",
        "pattern": r"stub\.\w+\s*\([^)]*\)(?!\s*\n.*(?:timeout|deadline|wait_for_ready))",
        "severity": "medium",
        "category": "config",
        "description": "gRPC client call without deadline/timeout in {file}. RPC can hang indefinitely causing goroutine/thread leaks.",
        "suggestion": "Set deadline on every call: stub.method(request, timeout=30.0) in Python, or ctx with deadline: ctx, cancel := context.WithTimeout(ctx, 30*time.Second); defer cancel() in Go.",
        "confidence": 0.60,
        "tags": ["OWASP:A04", "CWE-400"],
    },
    {
        "id": "grpc-streaming-no-backpressure",
        "pattern": r"for\s+\w+\s+in\s+\w+\.(?:stream|streaming_\w+)\s*\([^)]*\)(?![^:]*(?:limit|max|buffer|semaphore))",
        "severity": "medium",
        "category": "config",
        "description": "gRPC streaming without backpressure control in {file}. Fast producers can overwhelm slow consumers, exhausting memory.",
        "suggestion": "Implement flow control: use a semaphore or channel buffer to limit in-flight messages. For server streaming, check client health periodically.",
        "confidence": 0.55,
        "tags": ["OWASP:A04", "CWE-400"],
    },

    # ── WebAssembly / WASM ────────────────────────────────────────────────────
    {
        "id": "wasm-untrusted-source",
        "pattern": r"WebAssembly\.(?:instantiate|instantiateStreaming|compile)\s*\(\s*fetch\s*\(",
        "severity": "high",
        "category": "config",
        "description": "WebAssembly module loaded from fetched URL in {file}. Loading WASM from untrusted or user-controlled URLs enables code injection.",
        "suggestion": "Only load WASM from trusted, integrity-checked sources. Use Subresource Integrity (SRI) hashes. Validate the source URL against an allowlist.",
        "confidence": 0.72,
        "tags": ["OWASP:A08", "CWE-494"],
    },
    {
        "id": "wasm-missing-csp",
        "pattern": r"WebAssembly\.(?:instantiate|compile)\s*\(",
        "severity": "medium",
        "category": "config",
        "description": "WebAssembly used in {file} — ensure Content-Security-Policy includes 'wasm-unsafe-eval' or 'unsafe-eval' is not present.",
        "suggestion": "Set CSP header: Content-Security-Policy: script-src 'self'; wasm-src 'self'. Without CSP, XSS can lead to arbitrary WASM execution.",
        "confidence": 0.50,
        "tags": ["OWASP:A05", "CWE-16"],
    },
    {
        "id": "wasm-shared-memory-no-atomics",
        "pattern": r"new\s+SharedArrayBuffer\s*\([^)]*\)(?![^;]*Atomics\.)",
        "severity": "medium",
        "category": "crypto",
        "description": "SharedArrayBuffer used without Atomics synchronization in {file}. Race conditions can cause data corruption.",
        "suggestion": "Use Atomics.store/load/compareExchange for all SharedArrayBuffer access. Enable COOP/COEP headers required for SharedArrayBuffer: Cross-Origin-Opener-Policy: same-origin.",
        "confidence": 0.68,
        "tags": ["OWASP:A04", "CWE-362"],
    },

    # ── Cryptography weaknesses ───────────────────────────────────────────────
    {
        "id": "crypto-ecb-mode",
        "pattern": r"(?i)(?:AES|DES|Blowfish).*ECB|ECB.*(?:AES|DES|Blowfish)|Cipher\.getInstance\s*\(['\"]AES['\"](?!\s*/['\"]CBC|GCM)",
        "severity": "high",
        "category": "crypto",
        "description": "ECB mode encryption in {file}. ECB mode encrypts each block independently — identical plaintext blocks produce identical ciphertext, leaking data patterns.",
        "suggestion": "Use AES-GCM (authenticated encryption): Cipher.getInstance('AES/GCM/NoPadding') with a random 96-bit IV. Never use ECB mode for any sensitive data.",
        "confidence": 0.82,
        "tags": ["OWASP:A02", "CWE-327"],
    },
    {
        "id": "crypto-static-iv",
        "pattern": r"(?i)iv\s*=\s*(?:b?['\"][0-9a-fA-F]{16,32}['\"]|bytes\.fromhex\s*\(['\"][0-9a-fA-F]+['\"])|IvParameterSpec\s*\(\s*new\s+byte\s*\[",
        "severity": "high",
        "category": "crypto",
        "description": "Static/hardcoded IV for block cipher in {file}. Reusing IVs with the same key destroys confidentiality (nonce reuse attack).",
        "suggestion": "Generate a random IV for every encryption: iv = os.urandom(16). Prepend the IV to the ciphertext and extract it before decryption. Never hardcode or reuse IVs.",
        "confidence": 0.85,
        "tags": ["OWASP:A02", "CWE-330"],
    },
    {
        "id": "crypto-md5-integrity",
        "pattern": r"(?:hashlib\.md5|MD5\.digest|MessageDigest\.getInstance\s*\(['\"]MD5['\"]|crypto\.createHash\s*\(['\"]md5['\"])",
        "severity": "medium",
        "category": "crypto",
        "description": "MD5 used for integrity checking in {file}. MD5 is collision-prone and cannot be trusted for data integrity.",
        "suggestion": "Use SHA-256 or SHA-3: hashlib.sha256(), crypto.createHash('sha256'). For HMAC-based integrity use HMAC-SHA256.",
        "confidence": 0.78,
        "tags": ["OWASP:A02", "CWE-327"],
    },
    {
        "id": "crypto-sha1-collision-risk",
        "pattern": r"(?:hashlib\.sha1\s*\(|MessageDigest\.getInstance\s*\(['\"]SHA-1['\"]|crypto\.createHash\s*\(['\"]sha1['\"])",
        "severity": "medium",
        "category": "crypto",
        "description": "SHA-1 used in {file}. SHA-1 is vulnerable to chosen-prefix collision attacks (SHAttered, 2017).",
        "suggestion": "Migrate to SHA-256 or SHA-3. For certificates and signatures, SHA-1 has been deprecated by all major browsers and CAs.",
        "confidence": 0.80,
        "tags": ["OWASP:A02", "CWE-327"],
    },
    {
        "id": "crypto-rsa-key-too-small",
        "pattern": r"(?:RSA|generateKeyPair)\s*\(['\"]?(?:512|768|1024)['\"]?",
        "severity": "high",
        "category": "crypto",
        "description": "RSA key size less than 2048 bits in {file}. Keys < 2048 bits are considered insecure and factorable.",
        "suggestion": "Use RSA-2048 minimum, RSA-4096 for long-term security. Or migrate to ECC (P-256/P-384) which provides equivalent security with smaller keys.",
        "confidence": 0.88,
        "tags": ["OWASP:A02", "CWE-326"],
    },
    {
        "id": "crypto-des-3des-usage",
        "pattern": r"(?i)(?:DESede|DES/|TripleDES|\"DES\"|Cipher\.getInstance\s*\(['\"]DES['\"]|des3|triple.?des)",
        "severity": "high",
        "category": "crypto",
        "description": "DES or 3DES cipher used in {file}. DES has a 56-bit key (trivially brute-forceable). 3DES is deprecated and vulnerable to Sweet32.",
        "suggestion": "Replace with AES-256-GCM. DES/3DES are prohibited by NIST SP 800-131A for new systems.",
        "confidence": 0.85,
        "tags": ["OWASP:A02", "CWE-326"],
    },
    {
        "id": "crypto-rc4-usage",
        "pattern": r"(?i)(?:RC4|ARCFOUR|arc4|rc4_encrypt|rc4_decrypt|\"ARCFOUR\")",
        "severity": "critical",
        "category": "crypto",
        "description": "RC4 cipher used in {file}. RC4 has multiple statistical biases and is completely broken for modern security.",
        "suggestion": "Replace with AES-256-GCM or ChaCha20-Poly1305. RC4 was removed from TLS 1.2 and is prohibited by RFC 7465.",
        "confidence": 0.90,
        "tags": ["OWASP:A02", "CWE-327"],
    },
    {
        "id": "crypto-pbkdf2-weak-iterations",
        "pattern": r"pbkdf2\s*\([^)]*(?:[1-9]\d{0,3}|[1-9]\d{4,4}(?:(?<!10000)|(?<!20000)|(?<!50000)|(?<!99999)))\s*[,)]",
        "severity": "high",
        "category": "crypto",
        "description": "PBKDF2 with low iteration count in {file}. Fewer than 100,000 iterations makes brute-force attacks feasible on modern hardware.",
        "suggestion": "Use at least 600,000 iterations (OWASP 2023 recommendation) for PBKDF2-SHA256. Or switch to Argon2id which is memory-hard and more resistant to GPU attacks.",
        "confidence": 0.72,
        "tags": ["OWASP:A02", "CWE-916"],
    },
    {
        "id": "crypto-non-constant-time-compare",
        "pattern": r"(?:===|!==|==|!=)\s*(?:token|secret|hash|mac|signature|password|key)|\bstrcmp\s*\(\s*\$(?:token|secret|hash|mac|password)",
        "severity": "medium",
        "category": "crypto",
        "description": "Non-constant-time string comparison for sensitive value in {file}. Timing attacks can leak secret length or content.",
        "suggestion": "Use constant-time comparison: hmac.compare_digest(a, b) in Python, crypto.timingSafeEqual(a, b) in Node.js, hash_equals($a, $b) in PHP.",
        "confidence": 0.65,
        "tags": ["OWASP:A02", "CWE-208"],
    },
    {
        "id": "crypto-predictable-session-token",
        "pattern": r"(?:session_id|session_token|auth_token)\s*=\s*(?:str\s*\(|int\s*\()?(?:time\.|datetime\.|Date\.now\(\)|Math\.random\(\))",
        "severity": "high",
        "category": "auth",
        "description": "Predictable session token generated from time or Math.random() in {file}. Tokens can be guessed or predicted.",
        "suggestion": "Use a CSPRNG: secrets.token_urlsafe(32) in Python, crypto.randomBytes(32) in Node.js. Session tokens must be unguessable.",
        "confidence": 0.82,
        "tags": ["OWASP:A07", "CWE-330"],
    },
    {
        "id": "crypto-missing-forward-secrecy",
        "pattern": r"(?i)(?:RSA_WITH|TLS_RSA_WITH)(?!.*(?:ECDHE|DHE))",
        "severity": "medium",
        "category": "crypto",
        "description": "TLS cipher suite without forward secrecy (non-ephemeral key exchange) in {file}. Past sessions can be decrypted if the private key is ever compromised.",
        "suggestion": "Configure only cipher suites with ECDHE or DHE key exchange to ensure Perfect Forward Secrecy (PFS). Disable all static RSA key exchange suites.",
        "confidence": 0.72,
        "tags": ["OWASP:A02", "CWE-311"],
    },
    {
        "id": "crypto-client-side-key-derivation",
        "pattern": r"(?:CryptoJS\.PBKDF2|pbkdf2Sync|deriveKey)\s*\([^)]*(?:document\.|localStorage|sessionStorage|getElementById|window\.)",
        "severity": "high",
        "category": "crypto",
        "description": "Encryption key derived from DOM/client-side source in {file}. Client-side key derivation exposes the key material to XSS attacks.",
        "suggestion": "Perform key derivation server-side. If client-side encryption is required, derive keys from a strong server-issued secret, not from DOM values.",
        "confidence": 0.75,
        "tags": ["OWASP:A02", "CWE-321"],
    },
    {
        "id": "crypto-weak-bcrypt-rounds",
        "pattern": r"bcrypt\.(?:hash|hashpw|gensalt)\s*\([^)]*(?:[1-9]|10)\s*\)",
        "severity": "medium",
        "category": "crypto",
        "description": "bcrypt with low cost factor (< 12) in {file}. Low rounds make brute-force attacks faster.",
        "suggestion": "Use at least bcrypt cost factor 12: bcrypt.gensalt(rounds=12). As hardware speeds up, increase the factor (12-14 recommended for 2024+).",
        "confidence": 0.70,
        "tags": ["OWASP:A02", "CWE-916"],
    },
    {
        "id": "crypto-hardcoded-encryption-key",
        "pattern": r"(?i)(?:AES|encryption|encrypt).*key\s*=\s*['\"][a-fA-F0-9]{16,}['\"]",
        "severity": "critical",
        "category": "crypto",
        "description": "Hardcoded encryption key in {file}. Anyone with source access can decrypt all protected data.",
        "suggestion": "Store encryption keys in a KMS (AWS KMS, HashiCorp Vault) or at minimum in environment variables. Rotate keys periodically.",
        "confidence": 0.82,
        "tags": ["OWASP:A02", "CWE-321"],
    },

    # ── SSRF patterns ─────────────────────────────────────────────────────────
    {
        "id": "ssrf-fetch-user-url",
        "pattern": r"fetch\s*\(\s*(?:req\.|request\.|params\.|query\.|body\.|url\s*=|userUrl|inputUrl)",
        "severity": "high",
        "category": "ssrf",
        "description": "fetch() called with potentially user-controlled URL in {file}. Enables SSRF to access internal services.",
        "suggestion": "Validate URL against an allowlist of permitted domains. Block private IP ranges (10.x, 172.16-31.x, 192.168.x, 127.x, 169.254.x). Use a dedicated SSRF-safe HTTP client library.",
        "confidence": 0.78,
        "tags": ["OWASP:A10", "CWE-918"],
    },
    {
        "id": "ssrf-axios-user-url",
        "pattern": r"axios\.\w+\s*\(\s*(?:req\.|request\.|params\.|query\.|body\.|url|userUrl|inputUrl)",
        "severity": "high",
        "category": "ssrf",
        "description": "axios request with potentially user-controlled URL in {file}. Enables SSRF attacks.",
        "suggestion": "Parse the URL and validate the hostname against an allowlist. Reject URLs with private/loopback IPs. Consider using ssrf-req-filter npm package.",
        "confidence": 0.78,
        "tags": ["OWASP:A10", "CWE-918"],
    },
    {
        "id": "ssrf-cloud-metadata-endpoint",
        "pattern": r"169\.254\.169\.254|fd00:ec2::254|metadata\.google\.internal",
        "severity": "critical",
        "category": "ssrf",
        "description": "Cloud instance metadata endpoint referenced in {file}. If reachable via SSRF, attackers can steal IAM credentials.",
        "suggestion": "Block all requests to 169.254.169.254 at the application level and via security groups/VPC network ACLs. Use IMDSv2 (requiring session tokens) on AWS EC2.",
        "confidence": 0.95,
        "tags": ["OWASP:A10", "CWE-918"],
    },
    {
        "id": "ssrf-file-protocol",
        "pattern": r"(?:fetch|axios|request|urllib|http\.get)\s*\(['\"]file://",
        "severity": "critical",
        "category": "ssrf",
        "description": "file:// protocol in HTTP client call in {file}. Allows reading local files via SSRF.",
        "suggestion": "Reject all non-http/https URL schemes. Validate the scheme: if not url.startsWith('https://') throw error. Use URL parsing to check scheme before making requests.",
        "confidence": 0.92,
        "tags": ["OWASP:A10", "CWE-918"],
    },
    {
        "id": "ssrf-dns-rebinding-risk",
        "pattern": r"(?:new\s+URL|urllib\.parse\.urlparse|url\.parse)\s*\([^)]*(?:req\.|request\.|params\.|user|input)\s*\)(?![^;]*(?:hostname|host)\s*===)",
        "severity": "high",
        "category": "ssrf",
        "description": "URL parsed from user input without hostname validation in {file}. Vulnerable to DNS rebinding attacks.",
        "suggestion": "After parsing, resolve the hostname to IP and verify it is not in private ranges. Re-validate after DNS resolution to prevent DNS rebinding. Use a library like ssrf-check.",
        "confidence": 0.70,
        "tags": ["OWASP:A10", "CWE-918"],
    },
    {
        "id": "ssrf-internal-network-access",
        "pattern": r"(?:10\.\d{1,3}\.\d{1,3}\.\d{1,3}|192\.168\.\d{1,3}\.\d{1,3}|172\.(?:1[6-9]|2\d|3[01])\.\d{1,3}\.\d{1,3})",
        "severity": "medium",
        "category": "ssrf",
        "description": "Private/internal IP address hardcoded in {file}. If used with user-controlled URL building, enables SSRF to internal network.",
        "suggestion": "Block requests to all RFC1918 private IP ranges in your outbound HTTP proxy/library. Never allow user-controlled data to reach internal endpoints.",
        "confidence": 0.55,
        "tags": ["OWASP:A10", "CWE-918"],
    },
    {
        "id": "ssrf-redirect-following",
        "pattern": r"(?:maxRedirects\s*:\s*(?:[1-9]\d|[2-9])|followRedirects\s*:\s*true)(?![^}]*checkRedirect)",
        "severity": "medium",
        "category": "ssrf",
        "description": "HTTP client configured to follow redirects without restriction in {file}. Redirects can be used to bypass SSRF URL validation.",
        "suggestion": "Disable redirects or limit to 1-2 and re-validate the redirect destination URL. Attackers can point initial URL to a trusted domain that redirects to an internal IP.",
        "confidence": 0.65,
        "tags": ["OWASP:A10", "CWE-918"],
    },

    # ── Business Logic / Auth ─────────────────────────────────────────────────
    {
        "id": "auth-jwt-algorithm-none",
        "pattern": r"(?i)algorithm\s*[:=]\s*['\"]none['\"]|alg\s*[:=]\s*['\"]none['\"]",
        "severity": "critical",
        "category": "auth",
        "description": "JWT algorithm set to 'none' in {file}. The 'none' algorithm means the signature is not verified — any JWT is accepted.",
        "suggestion": "Always specify a strong algorithm: { algorithm: 'RS256' } or 'HS256'. Reject tokens with alg:none. Use a library that rejects alg:none by default.",
        "confidence": 0.95,
        "tags": ["OWASP:A07", "CWE-347"],
    },
    {
        "id": "auth-jwt-no-expiry",
        "pattern": r"jwt\.sign\s*\([^,]+,\s*[^,]+(?:,\s*\{[^}]*\})?(?!\s*\n.*expiresIn|[^}]*expiresIn)",
        "severity": "high",
        "category": "auth",
        "description": "JWT signed without expiresIn option in {file}. Tokens without expiry are valid forever — stolen tokens can be used indefinitely.",
        "suggestion": "Always set token expiry: jwt.sign(payload, secret, { expiresIn: '1h' }). Use short-lived access tokens (15m) with refresh tokens for long sessions.",
        "confidence": 0.75,
        "tags": ["OWASP:A07", "CWE-613"],
    },
    {
        "id": "auth-hardcoded-admin-bypass",
        "pattern": r"(?:if|===|==)\s*['\"](?:admin|root|superuser|administrator)['\"]",
        "severity": "high",
        "category": "auth",
        "description": "Hardcoded admin/role check in {file}. String comparison for privilege escalation is brittle and can be bypassed.",
        "suggestion": "Use a proper RBAC system with database-stored roles. Check permissions via: user.hasPermission('admin') rather than string equality on username.",
        "confidence": 0.70,
        "tags": ["OWASP:A01", "CWE-798"],
    },
    {
        "id": "auth-idor-user-supplied-id",
        "pattern": r"(?:findById|findOne|getById|getUserById)\s*\(\s*(?:req\.|request\.|params\.|query\.|body\.)\w*[Ii]d",
        "severity": "high",
        "category": "auth",
        "description": "Object lookup with user-supplied ID in {file} without ownership verification. Insecure Direct Object Reference (IDOR).",
        "suggestion": "Always verify ownership: find({ _id: params.id, userId: req.user.id }). Never trust user-supplied IDs without confirming the requesting user owns the resource.",
        "confidence": 0.72,
        "tags": ["OWASP:A01", "CWE-639"],
    },
    {
        "id": "auth-password-reset-token-in-url",
        "pattern": r"(?:reset|forgot|password).*token.*(?:GET|query|params|req\.query|request\.GET)",
        "severity": "medium",
        "category": "auth",
        "description": "Password reset token passed in URL query parameter in {file}. Tokens in URLs appear in logs, Referer headers, and browser history.",
        "suggestion": "Send reset tokens in the request body (POST form) or as a short-lived signed cookie. Tokens in URL paths (not query strings) are acceptable if logs are protected.",
        "confidence": 0.65,
        "tags": ["OWASP:A07", "CWE-598"],
    },
    {
        "id": "auth-session-fixation",
        "pattern": r"req\.session\.\w+\s*=.*(?:user|auth|login|authenticated)(?![^}]*(?:regenerate|session\.save|session\.id\s*=))",
        "severity": "high",
        "category": "auth",
        "description": "Session variable set after auth without session ID regeneration in {file}. Vulnerable to session fixation attacks.",
        "suggestion": "Regenerate session after login: req.session.regenerate(callback) in express-session. This issues a new session ID, invalidating any pre-auth fixed session.",
        "confidence": 0.65,
        "tags": ["OWASP:A07", "CWE-384"],
    },
    {
        "id": "auth-weak-password-requirements",
        "pattern": r"(?:password|passwd)\.(?:length|size)\s*[<>]=?\s*[1-5]\b",
        "severity": "medium",
        "category": "auth",
        "description": "Very weak minimum password length requirement in {file}. Minimum of 5 or fewer characters allows trivially guessable passwords.",
        "suggestion": "Enforce minimum 12 characters per NIST SP 800-63B. Check against known breached passwords (haveibeenpwned API). Avoid complexity rules that reduce entropy.",
        "confidence": 0.80,
        "tags": ["OWASP:A07", "CWE-521"],
    },
    {
        "id": "auth-missing-account-lockout",
        "pattern": r"(?:login|authenticate|checkPassword|verifyPassword)\s*\([^)]*\)(?![^}]{0,500}(?:lockout|attempts|rateLimit|throttle|429))",
        "severity": "medium",
        "category": "auth",
        "description": "Authentication function in {file} without apparent account lockout or rate limiting. Enables brute-force attacks.",
        "suggestion": "Implement progressive delays and account lockout: lock after 5 failed attempts for 15 minutes. Use Redis to track failed attempts per account/IP.",
        "confidence": 0.50,
        "tags": ["OWASP:A07", "CWE-307"],
    },
    {
        "id": "auth-email-enumeration",
        "pattern": r"(?:User not found|No account|Invalid email|Email not registered|does not exist)",
        "severity": "low",
        "category": "auth",
        "description": "Different error message for invalid email vs wrong password in {file}. Allows attackers to enumerate valid email addresses.",
        "suggestion": "Return the same error for both cases: 'Invalid email or password'. This prevents attackers from confirming which emails are registered.",
        "confidence": 0.70,
        "tags": ["OWASP:A07", "CWE-204"],
    },
    {
        "id": "auth-missing-object-level-auth",
        "pattern": r"router\.\w+\s*\(['\"][^'\"]+/:id[^'\"]*['\"][^)]*,\s*(?:async\s*)?\([^)]*\)\s*=>\s*\{(?![^}]{0,300}(?:req\.user|userId|ownership|authorized|permission))",
        "severity": "high",
        "category": "auth",
        "description": "Route with :id parameter in {file} may lack object-level authorization. Any authenticated user might access any resource by ID.",
        "suggestion": "Add ownership check in every handler: const resource = await Resource.findOne({ id: params.id, owner: req.user.id }); if (!resource) return 403.",
        "confidence": 0.58,
        "tags": ["OWASP:A01", "CWE-639"],
    },
    {
        "id": "auth-insecure-cookie-flags",
        "pattern": r"(?:res\.cookie|setCookie|Set-Cookie)\s*\([^)]*\)(?![^;)]*(?:httpOnly|HttpOnly)\s*:\s*true)(?![^;)]*(?:secure|Secure)\s*:\s*true)",
        "severity": "medium",
        "category": "auth",
        "description": "Cookie set without HttpOnly and Secure flags in {file}. Cookie accessible to JavaScript (XSS) and transmitted over HTTP.",
        "suggestion": "Set both flags: res.cookie('session', value, { httpOnly: true, secure: true, sameSite: 'Strict' }). Also set SameSite to prevent CSRF.",
        "confidence": 0.70,
        "tags": ["OWASP:A02", "CWE-614"],
    },
    {
        "id": "auth-jwt-weak-secret",
        "pattern": r"jwt\.sign\s*\([^,]+,\s*['\"][^'\"]{1,15}['\"]",
        "severity": "critical",
        "category": "auth",
        "description": "JWT signed with a very short secret in {file}. Secrets under 16 characters are trivially brute-forced.",
        "suggestion": "Use a randomly generated 256-bit (32-byte) secret: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\". Store in environment variable.",
        "confidence": 0.85,
        "tags": ["OWASP:A02", "CWE-321"],
    },
    {
        "id": "auth-bearer-token-log",
        "pattern": r"(?:console\.log|logger\.\w+|print|logging\.\w+)\s*\([^)]*(?:token|bearer|authorization|auth_token|access_token)",
        "severity": "high",
        "category": "auth",
        "description": "Authentication token being logged in {file}. Tokens in logs can be extracted and replayed by anyone with log access.",
        "suggestion": "Never log authentication tokens, passwords, or secrets. Redact sensitive headers: log only the first 4 chars or 'REDACTED'. Use structured logging with field-level redaction.",
        "confidence": 0.78,
        "tags": ["OWASP:A09", "CWE-312"],
    },
]

# Merge all pattern lists for convenience
ALL_PATTERNS = VULN_PATTERNS + EXTENDED_VULN_PATTERNS
